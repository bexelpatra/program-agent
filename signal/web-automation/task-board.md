# Task Board

| ID | Title | Assignee | Status | Priority | Depends On | Created | Updated |
|----|-------|----------|--------|----------|------------|---------|---------|
| --- | **Phase 1: Core Toolkit** | --- | --- | --- | --- | --- | --- |
| TASK-001 | 프로젝트 초기화 (requirements.txt, 디렉토리 구조, config 템플릿) | coder | DONE | HIGH | - | 2026-04-13T20:00 | 2026-04-13T20:35 |
| TASK-002 | Core - browser.py (Playwright 브라우저 엔진: 시작/종료/이동/대기/스크린샷) | coder | DONE | HIGH | TASK-001 | 2026-04-13T20:00 | 2026-04-13T20:50 |
| TASK-003 | Core - config.py (YAML 설정 로더) + settings.yaml 템플릿 | coder | DONE | HIGH | TASK-001 | 2026-04-13T20:00 | 2026-04-13T20:40 |
| TASK-004 | Core - telegram.py (텔레그램 봇 알림: token+chat_id 기반) | coder | DONE | HIGH | TASK-001 | 2026-04-13T20:00 | 2026-04-13T20:50 |
| TASK-005 | Core - logger.py (로깅 + 단계별 스크린샷 자동 저장) | coder | DONE | MEDIUM | TASK-001 | 2026-04-13T20:00 | 2026-04-13T20:50 |
| TASK-006 | Core - retry.py (재시도 데코레이터 + 에러 핸들링) | coder | DONE | MEDIUM | TASK-001 | 2026-04-13T20:00 | 2026-04-13T20:50 |
| TASK-007 | Core Toolkit 테스트 (browser, config, telegram, logger, retry) | tester | TODO | HIGH | TASK-002,TASK-003,TASK-004,TASK-005,TASK-006 | 2026-04-13T20:00 | 2026-04-13T20:00 |
| --- | **Phase 2: 인증 + Anti-Bot** | --- | --- | --- | --- | --- | --- |
| TASK-008 | auth/naver_imap.py (Naver IMAP 연결 → 인증번호 자동 추출) — `tistory.yaml:15-19` `naver_imap` 블록과 `account.email`/`account.password` 재사용, `settings.yaml` 신규 섹션 추가 금지. `tests/test_naver_imap.py` (imaplib.IMAP4_SSL mock 기반 단위 테스트) 포함. | coder | DONE | TOP | TASK-001 | 2026-04-13T20:00 | 2026-04-22T00:30 |
| TASK-008-UX | .env.example 템플릿 + scripts/smoke_naver_imap.sh (bash wrapper: .env 자동 로드 → python3 스모크 실행) | manager | DONE | TOP | TASK-008 | 2026-04-22T01:20 | 2026-04-22T01:20 |
| TASK-008-FIX | 스모크 결과 반영: (1) 정규식 기본값 `\b\d{6}\b` → `\b\d{6,8}\b` (카카오 인증번호 8자리 확인), (2) 카카오 계정 ≠ 네이버 IMAP 계정 분리: `account.*`=카카오, `naver_imap.email`/`naver_imap.password`=네이버, (3) `.env.example` 두 계정 필드 분리, (4) `tistory.yaml`/`naver_imap.py`/`test_naver_imap.py`/`smoke_naver_imap.sh` 일괄 수정. | coder | DONE | TOP | TASK-008 | 2026-04-22T09:35 | 2026-04-22T09:50 |
| TASK-008-SMOKE | 실계정 연결 스모크 테스트 (`cp .env.example .env` → 값 채우기 → `./scripts/smoke_naver_imap.sh` 실행) | user | TODO | TOP | TASK-008,TASK-008-UX | 2026-04-22T00:00 | 2026-04-22T01:20 |
| TASK-009 | antibot/stealth.py (playwright-stealth 설정 적용) | coder | TODO | MEDIUM | TASK-002 | 2026-04-13T20:00 | 2026-04-22T00:00 |
| TASK-010 | ~~antibot/cloudflare.py~~ (제거됨: stealth로 CF 우회 불가 확인, capsolver 유료 API 도입 시점까지 보류) | - | REMOVED | - | - | 2026-04-13T20:00 | 2026-04-22T00:00 |
| TASK-011 | 인증 + stealth 테스트 (naver_imap, stealth) | tester | TODO | HIGH | TASK-008,TASK-009 | 2026-04-13T20:00 | 2026-04-22T00:00 |
| --- | **Phase 3: 티스토리 자동화** | --- | --- | --- | --- | --- | --- |
| TASK-012 | sites/tistory/login.py (카카오 로그인 → 이메일 인증번호 입력) [**세분화됨** → TASK-012-ENV/A/B/C/D] | coder | SPLIT | HIGH | TASK-002,TASK-008 | 2026-04-13T20:00 | 2026-04-22T10:00 |
| TASK-012-ENV | `.env`에 카카오 계정 2개 변수 추가 (`WA_ACCOUNT_EMAIL`, `WA_ACCOUNT_PASSWORD`). `.env.example` L11-L12 템플릿 참조. 값은 사용자 카카오 ID/비밀번호 (티스토리 로그인용). | user | DONE | HIGH | - | 2026-04-22T10:00 | 2026-04-22T11:00 |
| TASK-012-D | 실계정 스모크 테스트 + 5건 bug/fix 발견 및 즉시 수정. **최종 결과**: exit 0, URL=`https://www.tistory.com/`, verify_logged_in=True. | user+manager | DONE | HIGH | TASK-012-C | 2026-04-22T10:00 | 2026-04-22T11:20 |
| TASK-012-FIX | 스모크 과정에서 발견한 3 bug + 2 fix 일괄 수정. **파일 변경**: (a) `src/sites/tistory/login.py` — verify_logged_in URL hostname 파싱 (urlparse) + 10초 리다이렉트 폴링, poll_verification_code 에 `code_pattern=rf"\b\d{{{code_length}}}\b"` 전달, switch_to_email_2fa 앞에 `wait_for_selector("text=이메일로 인증")` 대기 추가. (b) `config/tistory.yaml` — selectors.kakao_id_input=`input[name="loginId"]`, selectors.kakao_pw_input=`input[name="password"]` 실측 주입. **원인 분석**: ① 카카오 URL 의 URL-encoded continue 파라미터에 `tistory.com` 문자열이 포함되어 기존 `"tistory.com" in url` 매치가 false-positive 유발. ② `email_2fa.code_length=8` 설정값이 로그에만 기록되고 정규식에 반영되지 않아 `\b\d{6,8}\b` 기본 패턴이 메일 본문의 6자리 숫자(타임스탬프)를 먼저 매치. ③ submit_code 직후 카카오 → kauth → tistory 리다이렉트 체인 완료 전에 verify 호출. ④ fill_kakao 이후 2FA 화면 렌더 완료 전 switch_to_email_2fa 의 `get_by_role().count()` 가 0 반환. ⑤ 카카오 로그인 입력란은 `type="text"[name="loginId"]` (type=email 아님). | coder(manager-inline) | DONE | HIGH | TASK-012-D | 2026-04-22T11:00 | 2026-04-22T11:20 |
| TASK-012-A | `projects/web-automation/src/sites/tistory/login.py` 신규 구현. **클래스**: `TistoryKakaoLogin`(config: Config, browser: BrowserManager). **메서드 분해**: `open_login_page()` → `https://www.tistory.com/auth/login` 방문 + "카카오 계정으로 로그인" 버튼 클릭 (셀렉터는 `selectors.login_kakao_button` 우선, 공란이면 `page.get_by_text("카카오")` role=button/link fallback) / `fill_kakao_credentials()` → 카카오 로그인 폼에 `account.email`·`account.password` 입력 + 로그인 버튼 클릭 (`selectors.kakao_id_input`/`kakao_pw_input` 우선, 공란이면 input[type=email]/input[type=password]+submit fallback) / `switch_to_email_2fa()` → `selectors.email_2fa_button` 우선, **공란이면 HTML DOM 텍스트 파싱** fallback: `page.get_by_text("이메일")` + role=button/link filter → 후보 수집 후 "이메일로 인증하기"·"이메일 인증" 완전일치 우선, 그다음 visible+enabled 우선 단일 선택 후 클릭. **OCR 금지** — 오직 DOM 텍스트/role/aria. / `poll_verification_code(sender_patterns: list[str], max_attempts=20, interval=3)` → **리스트 순회**하며 각 sender 문자열을 `naver_imap.fetch_verification_code(config, sender=s, within_minutes=email_2fa.within_minutes)` 로 호출 (단일 str 인자 — `fetch_verification_code` 시그니처는 `naver_imap.py:162-168` 기준 `sender: str`). 첫 hit 반환. / `submit_code(code)` → `selectors.auth_code_input` 우선, 공란이면 input[inputmode=numeric]/input[maxlength=8] fallback 후 채우고 제출 / `verify_logged_in()` → URL 리다이렉트 또는 관리자 영역 요소로 성공 확인. **실패 시 각 단계 스크린샷 저장** (`screenshots/tistory_login_{step}.png`, BrowserManager.screenshot 사용). **[공유 Config key 계약]** (TASK-012-B 와 원자적 일치 필요): `selectors.login_kakao_button` / `selectors.kakao_id_input` / `selectors.kakao_pw_input` / `selectors.email_2fa_button` / `selectors.auth_code_input` / `email_2fa.sender_patterns` (list[str]) / `email_2fa.code_length` (int, default 8) / `email_2fa.within_minutes` (int, default 5). **기존 yaml 의 selectors.login_email_input/login_password_input/login_submit_button 은 이 흐름에서 사용 안 함** (티스토리 자체 로그인용 키 — 유지만). | coder | DONE | HIGH | - | 2026-04-22T10:00 | 2026-04-22T10:50 |
| TASK-012-B | `projects/web-automation/config/tistory.yaml` 보강: (a) `selectors` 섹션에 **신규 5키 추가** (`login_kakao_button`/`kakao_id_input`/`kakao_pw_input`/`email_2fa_button`/`auth_code_input` — 실측 전까지 공란 유지, login.py 가 DOM fallback 사용). **기존 7키(login_email_input·login_password_input·login_submit_button·auth_code_input·write_title_input·write_content_area·write_publish_button) 는 유지** (write 계열은 TASK-013 에서 사용, 기존 login_* 은 티스토리 자체 로그인 경로 예비). 중복 키(`auth_code_input`)는 기존 값 그대로 사용 (신규 키 아님 — yaml 현재 L34 에 존재). (b) **신규 최상위 섹션 `email_2fa`** 추가 (`kakao_auth` 아님): `email_2fa.sender_patterns` 리스트 (기본 `["noreply@kakaocorp.com", "accounts.kakao.com", "kakao.com"]` — 스모크에서 실측 후 확정), `email_2fa.code_length: 8`, `email_2fa.within_minutes: 5`. 이유: 이 설정은 "카카오 인증 방식"이 아닌 "카카오가 보내는 인증 메일의 특성"이므로 향후 다른 사이트의 email 2FA 로도 재사용 가능한 네이밍 선택. **[공유 Config key 계약]** (TASK-012-A 와 동일): `selectors.login_kakao_button` / `selectors.kakao_id_input` / `selectors.kakao_pw_input` / `selectors.email_2fa_button` / `selectors.auth_code_input` / `email_2fa.sender_patterns` / `email_2fa.code_length` / `email_2fa.within_minutes`. | coder | DONE | HIGH | - | 2026-04-22T10:00 | 2026-04-22T10:30 |
| TASK-012-C | 티스토리 로그인 스모크 러너 작성. **산출물**: (1) `projects/web-automation/scripts/smoke_tistory_login.sh` (bash wrapper), (2) `projects/web-automation/scripts/smoke_tistory_login.py` (python 엔트리, **별도 파일** — 기존 `smoke_naver_imap.sh` 의 heredoc 인라인 방식 **재사용 불가**). **(A) bash wrapper 동작**: ① `set -euo pipefail` + `cd` to PROJECT_ROOT. ② `.env` 존재 확인 → 없으면 `cp .env.example .env` 안내 출력 후 exit 1. ③ `.env` 4필드 전부 검증: `WA_ACCOUNT_EMAIL`/`WA_ACCOUNT_PASSWORD`/`WA_NAVER_IMAP_EMAIL`/`WA_NAVER_IMAP_PASSWORD` 모두 비어있지 않은지 확인 (기존 `smoke_naver_imap.sh` L39-46 2필드 검증 패턴 확장). ④ `.env` export (기존 패턴 재사용). ⑤ **headful 강제**: `export WA_BROWSER_HEADLESS=false` — `settings.yaml` 은 이미 `headless: false` (L12) 이나 방어적으로 env override 로 확정. Config.get() 의 WA_ 환경변수 우선권은 `src/core/config.py:101-104` 실측 기준. `BrowserManager._headless` private override 같은 안티패턴 금지. ⑥ **playwright chromium 바이너리 선행 체크**: `python3 -c "from playwright.sync_api import sync_playwright; sync_playwright().start().chromium.executable_path" 2>/dev/null` 실행해 비정상 종료 시 "playwright install chromium 을 먼저 실행하세요" echo 후 exit 2. ⑦ `PYTHONPATH="$(pwd)" python3 scripts/smoke_tistory_login.py` 실행. **(B) python 엔트리 흐름**: ① 파일 상단에 `import sys; from pathlib import Path; sys.path.insert(0, str(Path(__file__).resolve().parents[1]))` 로 PROJECT_ROOT 를 sys.path 에 추가 (엔트리 파일 독립 실행 대비 방어). ② `from src.core.config import Config` / `from src.core.browser import BrowserManager` / `from src.sites.tistory.login import TistoryKakaoLogin` / `from src.core.logger import get_logger`. ③ `async def main() -> int:` 정의 — Config 인스턴스화 후 `config.load_site("tistory")` (실측: `config.py:74`) → `async with BrowserManager(config) as browser:` → `result = await TistoryKakaoLogin(config, browser).run()` → `result=True` 면 "✅ 로그인 성공" + `screenshots/` 경로 안내 후 `return 0`, `False` 면 "❌ 로그인 실패 — 스크린샷 확인: screenshots/tistory_login_*.png" 후 `return 1`. ④ 최상위 try/except 로 `logger.exception("스모크 실패")` + `return 2` (예외). ⑤ `if __name__ == "__main__": sys.exit(asyncio.run(main()))`. **종료 코드 규약**: 0=성공 / 1=로그인 실패(verify_logged_in False) / 2=예외·설정 오류. **실측 참조**: `login.py` 466 lines, `TistoryKakaoLogin(config: Config, browser: BrowserManager)`, `run() -> bool` (coder-report-TASK-012-A.md 확정). **완료 조건**: 두 파일 신규 생성 + `bash -n scripts/smoke_tistory_login.sh` 문법 체크 통과 + `python3 -m py_compile scripts/smoke_tistory_login.py` 통과 + `grep -E "_headless" scripts/smoke_tistory_login.py` 0건 (안티패턴 방지 실증) + 4필드 검증 블록이 bash wrapper 에 존재함 grep 실증. | coder | DONE | HIGH | TASK-012-A,TASK-012-B | 2026-04-22T10:00 | 2026-04-22T11:10 |
| TASK-012-D-ORIG | (SUPERSEDED by 상단 TASK-012-D DONE row — duplicate 제거 대기) | user | DONE | HIGH | TASK-012-C | 2026-04-22T10:00 | 2026-04-22T11:20 |
| TASK-012-C-XVFB | `smoke_tistory_login.sh` 에 DISPLAY 자동 감지 + xvfb-run 자동 래핑 추가. 재귀 방지 가드(`_WA_SMOKE_XVFB_WRAPPED`) 포함. xvfb-run 미설치 시 설치 안내 warn 후 계속 진행. bash -n + 실제 실행 exit 0 검증 완료. | coder(manager-inline) | DONE | MEDIUM | TASK-012-D | 2026-04-22T11:25 | 2026-04-22T11:30 |
| TASK-013 | sites/tistory/writer.py (글 작성 자동화: 제목/본문/카테고리/발행) [**세분화됨** → TASK-013-A/B/C/D] | coder | SPLIT | HIGH | TASK-012 | 2026-04-13T20:00 | 2026-04-22T11:35 |
| TASK-013-A | `projects/web-automation/src/sites/tistory/writer.py` 신규 구현. **클래스**: `TistoryWriter(config: Config, browser: BrowserManager)`. **모듈 상단 상수/헬퍼 재정의 (login.py 복제, 간단 경로)**: `_STEP_TIMEOUT_MS = 15000` 정의 + 내부 헬퍼 `async def _screenshot_on_error(self, step: str)` (login.py 의 동일 로직 복제, 향후 공통 모듈 추출은 별도 리팩터 태스크). **메서드 분해 6개 + run 1개**: **① `open_newpost()`** → `blog_name = config.get("blog.blog_name")` (None/빈값 시 `ValueError("blog.blog_name 이 필요합니다")`). `page.goto(f"https://{blog_name}.tistory.com/manage/newpost")` + `page.wait_for_selector("#post-title-inp", timeout=_STEP_TIMEOUT_MS)` + TinyMCE init 대기 `await page.wait_for_function("() => window.tinymce && window.tinymce.activeEditor && window.tinymce.activeEditor.getBody()", timeout=_STEP_TIMEOUT_MS)`. **② `fill_title(title)`** → `await page.fill("#post-title-inp", title)`. **③ `set_body_html(html)`** → `await page.evaluate("(h) => window.tinymce.activeEditor.setContent(h)", html)` + **검증**: `await page.wait_for_function("() => window.tinymce.activeEditor.getContent().length > 0", timeout=_STEP_TIMEOUT_MS)` (실측: 티스토리 스킨이 `data-ke-size` 등 속성 자동 추가하나 HTML 구조/텍스트 보존). **④ `add_tags(tags: list[str])`** → 각 태그를 `await page.fill("#tagText", tag)` + `await page.press("#tagText", "Enter")` + **확정 대기**: `await page.wait_for_timeout(200)` (태그 칩 렌더 대기). **⑤ `select_category(name: str | None)`** → `name=None` 이면 즉시 return. `await page.click("#category-btn")` → `await page.wait_for_selector("#category-list", timeout=_STEP_TIMEOUT_MS)` → `option = page.locator('#category-list [aria-label]', has_text=re.compile(rf"^{re.escape(name)}$"))` — **aria-label 부분일치 위험 차단**. `count = await option.count()` 가 0 이면 `ValueError(f"카테고리 '{name}' 없음. 사용 가능: ...")` 와 함께 전체 aria-label 리스트 노출. 매치되면 `await option.first.click()`. 실측 카테고리 예시 (perlky): "카테고리 없음"/"차트공부"/"메모"/"분석 연습하기"/"여행 기록"/"영화감상"/"잡념 상자" — 블로그별 상이. **⑥ `attach_file(path: str \| Path, kind: str = "file")`** → path=None 이면 skip. kind→text 매핑: `{"file": "파일", "image": "사진", "slideshow": "슬라이드쇼"}`. (1) 첨부 드롭다운 열기: `await page.locator('div[role="button"][title="첨부"]:visible').first.click()` (실측: DOM 에 3개 동일 요소, 1개만 offsetParent !== null). (2) **file_chooser 순서 중요** — 아래 코드 블록 순서 그대로 구현: `menu_item = page.locator('.mce-tistory-attach-item', has_text=re.compile(rf"^{re.escape(text_for_kind)}$"))` (**completeness: substring 오매치 방지 — `:has-text("파일")` 은 '슬라이드쇼 파일' 같은 조합에 오매치 가능**). 그 다음 `async with page.expect_file_chooser() as fc_info: await menu_item.first.click()` → `chooser = await fc_info.value` → `await chooser.set_files(str(Path(path).resolve()))`. (3) 업로드 완료 대기: `await page.wait_for_load_state("networkidle", timeout=_STEP_TIMEOUT_MS)` + `await asyncio.sleep(3)`. **⑦ `save_draft()`** → `await page.locator("a.action", has_text=re.compile(r"^임시저장$")).first.click()`. **성공 판정 단순화 (Reviewer #4 택B)**: 클릭 후 `await page.wait_for_load_state("networkidle", timeout=_STEP_TIMEOUT_MS)` + `await asyncio.sleep(2)`. 예외 없이 종료되면 True 반환. **실제 임시저장 성공 여부는 TASK-013-D (사용자 육안 검증) 에서 확인**. **예외 처리**: 각 메서드는 try/except 에서 `_screenshot_on_error(step)` 후 재raise (login.py 동일 패턴). `select_category`/`attach_file` 은 인자 None/빈값 시 조용히 skip. **공개 진입점**: `async def run(self, *, title: str, body_html: str, tags: list[str] \| None = None, category: str \| None = None, category_fallback: str \| None = None, attachments: list[str] \| None = None, mode: str = "draft") -> bool`. **mode 분기**: mode="draft" → save_draft / mode="publish" → `raise NotImplementedError("publish mode 는 후속 태스크에서 구현")` (silently skip 금지). **category_fallback 동작** (Reviewer #10): `select_category(category)` 가 ValueError 시 `category_fallback` 이 None 이 아니면 `select_category(category_fallback)` 재시도, 그것도 실패면 최종 raise. **OCR 금지**. **Config key 계약 10개** (TASK-013-B 와 원자적 일치): `blog.blog_name` / `selectors.write_title_input` / `selectors.write_body_iframe` / `selectors.tag_input` / `selectors.category_btn` / `selectors.category_list` / `selectors.attach_btn` / `selectors.attach_option_prefix` / `selectors.save_draft_btn` / `selectors.publish_btn`. | coder | DONE | HIGH | - | 2026-04-22T11:35 | 2026-04-22T12:00 |
| TASK-013-B | `projects/web-automation/config/tistory.yaml` 보강. **(a) `blog.blog_name: perlky`** 실측 주입 (기존 placeholder `YOUR_BLOG_NAME` 교체). **(b) `selectors` 섹션 변경** — **Reviewer #1 택B 확정 경로**: ① 기존 L44 `write_title_input: ""` 의 **값**을 `'textarea#post-title-inp'` 로 **덮어쓰기** (key 이름 유지). ② 기존 L45 `write_content_area: ""` **삭제** (신규 계약에 해당 이름 없음 — 본문은 TinyMCE API 경유이므로 selector 불필요). ③ 기존 L46 `write_publish_button: ""` 의 **key 이름을 `publish_btn` 으로 rename** + 값 `'button#publish-layer-btn'` 주입. ④ 신규 key 7개 추가: `write_body_iframe: 'iframe[name="editor-tistory_ifr"]'` (fallback 용) / `tag_input: 'input#tagText'` / `category_btn: 'button#category-btn'` / `category_list: '#category-list'` / `attach_btn: 'div[role="button"][title="첨부"]'` / `attach_option_prefix: '.mce-tistory-attach-item'` / `save_draft_btn: 'a.action'` (text="임시저장" 매치는 코드측). **[공유 Config key 계약 10개]** (TASK-013-A 와 동일): `blog.blog_name` / `selectors.write_title_input` / `selectors.write_body_iframe` / `selectors.tag_input` / `selectors.category_btn` / `selectors.category_list` / `selectors.attach_btn` / `selectors.attach_option_prefix` / `selectors.save_draft_btn` / `selectors.publish_btn`. **완료 조건** (Reviewer #1): (1) `python3 -c "import yaml; yaml.safe_load(open('config/tistory.yaml'))"` 파싱 성공 (yaml 동일 key 중복 없음 검증). (2) `grep -cE '^\s*write_content_area:' config/tistory.yaml` → **0** (삭제 확인). (3) `grep -cE '^\s*write_publish_button:' config/tistory.yaml` → **0** (rename 확인). (4) `grep -cE '^\s*publish_btn:' config/tistory.yaml` → **1**. (5) 공유 10개 key 각각 grep 으로 1 hit 실증 (보고서에 명령·결과). | coder | DONE | HIGH | - | 2026-04-22T11:35 | 2026-04-22T12:00 |
| TASK-013-C | `projects/web-automation/scripts/smoke_tistory_write.sh` + `smoke_tistory_write.py` 신규 스모크 러너. **(A) bash wrapper**: `smoke_tistory_login.sh` 구조 그대로 재사용 (set -euo pipefail, xvfb-run auto-detect, .env 4필드 검증, WA_BROWSER_HEADLESS=false, playwright chromium 선행 체크, PYTHONPATH + python3 실행). **샘플 파일 생성은 bash wrapper 책임** (Reviewer #9 확정): `.env` 로드 직후 `mkdir -p samples && [ -f samples/sample.txt ] || echo '테스트 첨부파일 — 티스토리 스모크' > samples/sample.txt` 한 줄. **(B) python 엔트리**: `smoke_tistory_login.py` 구조 복제 + 로그인 성공 후 `TistoryWriter(config, browser).run(...)` 호출. **샘플 데이터**: 제목=`"[스모크] 티스토리 자동화 테스트"` / 본문=`"<h2>스모크 테스트</h2><p>본문 <b>굵게</b> + 기울임 <i>italic</i>.</p><ul><li>항목 1</li><li>항목 2</li></ul>"` / 태그=`["스모크", "자동화"]` / 카테고리=`"메모"` / **`category_fallback="카테고리 없음"`** (Reviewer #10 확정 — writer.run() 에 인자로 전달, 스모크 자체에 try/except 금지). / 첨부=`["samples/sample.txt"]` / mode="draft". **종료 코드**: 0=임시저장 단계까지 예외 없이 완료 / 1=writer.run() False 반환(논리적으로 미도달 — `save_draft` 는 현재 True 만 반환) / 2=로그인/설정/예외. **사용자 육안 검증 문구 출력** (성공 시): "✅ 임시저장 요청 전송 완료. 관리자 페이지 '임시저장된 글' 목록에서 제목·본문·카테고리·태그·첨부파일을 확인하세요." | coder | TODO | HIGH | TASK-013-A,TASK-013-B | 2026-04-22T11:35 | 2026-04-22T11:50 |
| TASK-013-D | 사용자 임시저장 검증: `./scripts/smoke_tistory_write.sh` 실행 → 티스토리 관리 페이지 글 목록에서 "임시저장된 글"에 스모크 글 확인 → 카테고리·태그·첨부파일·본문 HTML 구조 확인 → 결과 공유. | user | TODO | HIGH | TASK-013-C | 2026-04-22T11:35 | 2026-04-22T11:35 |
| TASK-014 | tistory.yaml 설정 파일 작성 (URL, 셀렉터, 계정 설정 구조) | coder | TODO | HIGH | TASK-003 | 2026-04-13T20:00 | 2026-04-13T20:00 |
| TASK-015 | 티스토리 통합 테스트 (로그인 → 글 작성 → 발행 확인) | tester | TODO | HIGH | TASK-013,TASK-014 | 2026-04-13T20:00 | 2026-04-13T20:00 |
| --- | **Phase 4: 야놀자 자동화 (BLOCKED - capsolver 도입 대기)** | --- | --- | --- | --- | --- | --- |
| TASK-016 | sites/yanolja/login.py (Cloudflare 통과 → 이메일 로그인) | coder | BLOCKED | LOW | TASK-002,capsolver | 2026-04-13T20:00 | 2026-04-22T00:00 |
| TASK-017 | sites/yanolja/search.py (숙소 검색: 지역/날짜/인원/조건 필터) | coder | BLOCKED | LOW | TASK-016 | 2026-04-13T20:00 | 2026-04-22T00:00 |
| TASK-018 | sites/yanolja/booking.py (예약 버튼 클릭 + 텔레그램 알림) | coder | BLOCKED | LOW | TASK-017,TASK-004 | 2026-04-13T20:00 | 2026-04-22T00:00 |
| TASK-019 | yanolja.yaml 설정 파일 작성 (URL, 셀렉터, 계정 설정 구조) | coder | BLOCKED | LOW | TASK-003 | 2026-04-13T20:00 | 2026-04-22T00:00 |
| TASK-020 | 야놀자 통합 테스트 (CF 통과 → 로그인 → 검색 → 예약 클릭) | tester | BLOCKED | LOW | TASK-018,TASK-019 | 2026-04-13T20:00 | 2026-04-22T00:00 |
| --- | **Phase 5: CLI + 마무리** | --- | --- | --- | --- | --- | --- |
| TASK-021 | cli.py (CLI 진입점: tistory-login, tistory-write, yanolja-search, yanolja-book) | coder | TODO | MEDIUM | TASK-013,TASK-018 | 2026-04-13T20:00 | 2026-04-13T20:00 |
| TASK-022 | sites/base.py (BaseSiteWorkflow 인터페이스 정의) | coder | TODO | LOW | TASK-013,TASK-018 | 2026-04-13T20:00 | 2026-04-13T20:00 |
| TASK-023 | 전체 파이프라인 통합 테스트 | tester | TODO | HIGH | TASK-021 | 2026-04-13T20:00 | 2026-04-13T20:00 |
| --- | **Phase 6: 티스토리 글쓰기 에이전트 (architecture §6 참조)** | --- | --- | --- | --- | --- | --- |
| TASK-024 | 글쓰기 에이전트 상세 태스크 분해 (style/seo/image_placer/agent 등) | manager | TODO | LOW | TASK-013 | 2026-04-22T00:00 | 2026-04-22T00:00 |
| --- | **Phase 7: 폴더 기반 임시저장 자동화 (user 요청 — 2026-04-22)** | --- | --- | --- | --- | --- | --- |
| TASK-025 | 폴더 기반 글 조립 + 임시저장 자동화 (세분화 umbrella). 입력: `projects/web-automation/posts/{YYYY-MM-DD}-{slug}/` 폴더에 `post.md` (YAML frontmatter + 본문 with `${N}`·`${filename}` 마커) + 이미지 파일들 + 완료 시 `.published` 마커. 흐름: (1) 이미지 전체 업로드 `POST /manage/post/attach.json` → 매크로 URL 수집 / (2) 본문 마커 치환해 완성 HTML 생성 / (3) `POST /manage/drafts` 로 draft 생성 / (4) `.published` 마커 기록. 실패 시 완전 롤백 — 이미지 삭제 API 존재 시 사용, 없으면 orphan 목록 로깅 + 사용자 수동 정리. | manager | SPLIT | HIGH | TASK-013 | 2026-04-22T15:00 | 2026-04-22T15:00 |
| TASK-025-PROBE | Playwright Network 트레이싱으로 tistory 3 API 실측. **산출물**: `signal/web-automation/probe-tistory-api.md`. 핵심 발견: attach.json 응답(`key`/`url`) → 매크로 조립 공식 / drafts API schema `{title/content/tags/categoryId/thumbnail/draftSequence?}` / 이미지 삭제 API 부재 (orphan log 대체 확정) / multipart 필드명은 구현 시 추가 probe 필요. | coder(manager-inline) | DONE | HIGH | - | 2026-04-22T15:00 | 2026-04-22T15:10 |
| TASK-025-A0 | `projects/web-automation/src/tistory_post/models.py` 신규 — 공통 dataclass + 예외 (**A/B/C/D/E 선행, 이 이후 B/C/D 병렬 가능**). **LoadedPost**: `title: str` (필수), `category_name: str \| None`, `tags: list[str]` (빈 리스트 허용), `body_markdown: str` (비어있으면 loader 가 ValueError), `markers: list[Marker]`, `images: dict[str, Path]` (filename → path), `folder: Path`, `skip: bool` (.published 존재 시 True). **Marker**: `raw_token: str`, `kind: Literal["index","filename"]`, `value: int \| str`, `position: int` (본문 내 시작 offset). **UploadedImage**: `key: str`, `url: str`, `filename: str` (원본 파일명), `size: int`, `width: int`, `height: int`, `macro: str` (**`&amp;` escape 완료 상태**). **DraftPayload** (probe §2 명명 준수 — `content_html` 금지): `title: str`, `content: str` (HTML), `tags: str` (쉼표 구분), `categoryId: int` (0=없음), `thumbnail: str \| None`, `draftSequence: int \| None`. **RunResult**: `skipped: bool`, `draft_sequence: int \| None`, `orphan_images: list[UploadedImage]`, `error: str \| None`. **PartialUploadError(Exception)**: `uploaded: list[UploadedImage]`, `failed_index: int`, `cause: Exception`. 전부 `@dataclass` 사용, `from __future__ import annotations` + Python 3.11+ 타입. | coder | TODO | HIGH | TASK-025-PROBE | 2026-04-22T15:20 | 2026-04-22T15:20 |
| TASK-025-SETUP | `projects/web-automation/requirements.txt` 에 `Pillow>=10.0.0` + `markdown>=3.5` 추가. `pip install -r requirements.txt` 로 설치 확인 후 `python3 -c "import PIL; import markdown; print(PIL.__version__, markdown.__version__)"` 통과 보고. | coder | TODO | HIGH | - | 2026-04-22T15:20 | 2026-04-22T15:20 |
| TASK-025-A | `projects/web-automation/src/tistory_post/post_loader.py` 신규. **함수**: `load_post(folder: Path) -> LoadedPost`. models.py (TASK-025-A0) 의 LoadedPost/Marker 사용. **파싱 규칙**: (1) frontmatter YAML `---` 블록에서 `title`/`category`/`tags` 추출. (2) 본문 `${...}` 마커 정규식 `\$\{([^}]*)\}` 추출 (순서·position 보존). (3) 폴더 내 이미지 파일 (png/jpg/jpeg/gif/webp) **사전순 정렬**. (4) Marker 매핑 + 매핑 결과 검증. (5) `.published` 존재 → `skip=True` 로 즉시 반환. **마커 edge case 명세** (M5 반영): `${}` → ValueError("빈 마커"). `${0}`/음수 → ValueError("1-based 양의 정수"). `${1.5}`/`${abc}` → filename 취급 (확장자 포함 파일 매칭 시도, 없으면 ValueError). `${filename.png}` 파일 부재 → ValueError(`"이미지 파일 없음: {filename}. 폴더 내: {list}"`). 동일 마커 중복 **허용**. 고아 마커 (`${5}` 인데 이미지 4개) → ValueError. 고아 이미지 → warning 로그 + 허용. **frontmatter 규칙**: title 누락 → ValueError. category 누락 → None. tags 누락 → `[]`. body 비어있음 → ValueError. **산출물**: `tests/test_post_loader.py` 단위 테스트 (위 8+ 케이스 커버). | coder | TODO | HIGH | TASK-025-A0,TASK-025-SETUP | 2026-04-22T15:20 | 2026-04-22T15:20 |
| TASK-025-B | `projects/web-automation/src/tistory_post/image_uploader.py` 신규. **함수**: `async def upload_images(page, image_paths: list[Path]) -> list[UploadedImage]`. **구현**: 로그인된 Page 사용, `page.evaluate` 로 `FormData` + `fetch('/manage/post/attach.json', {method:'POST', body:form})` 호출. **multipart 필드명 probe 절차** (M4 반영): ① `file` 필드명 시도 → 응답 JSON 에 `key`·`url` 필드 존재하면 성공. ② 400/500 또는 필드 부재 시 `Filedata` 필드명으로 재시도. ③ 두 번 모두 실패 시 `page.route("**/attach.json", ...)` 로 브라우저 실제 요청 가로채 필드명 capture 후 재시도. ④ 최종 실패 → `RuntimeError("multipart field name unknown — probe-tistory-api.md L8 수동 탐색 필요")` raise. **매크로 조립** (probe §1 공식 + M3): `[##_Image|kage@{key}?{url_params_with_&amp;_escape}|CDM|1.3|{"originWidth":W,"originHeight":H,"style":"alignCenter","filename":"{filename}"}_##]` — 여기서 `url` query string 의 `&` → `&amp;` escape 를 macro 문자열에 **반드시 포함** (caller 가 재escape 금지). **이미지 크기**: `Pillow>=10` 의 `Image.open(path).size` 사용 (webp/jpeg/png/gif 전부 대응). **부분 실패**: k-th 이미지 실패 시 `PartialUploadError(uploaded=[...k-1개...], failed_index=k, cause=exc)` raise. caller (post_runner) 가 `.orphan.log` 처리. | coder | TODO | HIGH | TASK-025-A0,TASK-025-SETUP | 2026-04-22T15:20 | 2026-04-22T15:20 |
| TASK-025-CAT | `projects/web-automation/src/tistory_post/category_fetcher.py` 신규 (M6: post_saver 에서 분리). **함수**: `async def fetch_category_map(page) -> dict[str, int]`. **구현**: `page.goto('/manage/newpost')` 보장 (이미 로드돼 있으면 스킵) → `page.click("#category-btn")` → `page.wait_for_selector("#category-list")` → `page.evaluate` 로 `#category-list [aria-label]` 순회하며 각 요소의 `aria-label` 속성과 `category-id` 속성을 dict 로 조립. 빈 옵션(카테고리 없음 외) 이 없으면 warning 로그 후 `{"카테고리 없음": 0}` 반환. | coder | TODO | HIGH | TASK-025-A0 | 2026-04-22T15:20 | 2026-04-22T15:20 |
| TASK-025-C | `projects/web-automation/src/tistory_post/post_builder.py` 신규. **함수**: `build_payload(post: LoadedPost, uploads: list[UploadedImage], category_map: dict[str, int]) -> DraftPayload`. **단계** (M3/M10 반영): (1) `markdown.markdown(post.body_markdown, extensions=['extra', 'nl2br'])` 로 HTML 변환. (2) `${...}` 각 마커를 대응하는 UploadedImage.macro 로 **문자열 치환** (이미 `&amp;` escape 완료 상태라 재escape 금지). (3) `category_map.get(post.category_name, 0)` 로 categoryId 확정 (category_name=None 이면 0). 미매치 시 ValueError 로 사용 가능 목록 노출. (4) `tags` = `",".join(post.tags)`. (5) `thumbnail` = `uploads[0].key` if uploads else None. (6) DraftPayload 반환 (draftSequence 는 caller 가 주입, 여기서는 None). **테스트**: `tests/test_post_builder.py` — 마커 치환 정확성, empty uploads 시 thumbnail=None, 카테고리 미매치 시 ValueError 커버. | coder | TODO | HIGH | TASK-025-A0,TASK-025-SETUP | 2026-04-22T15:20 | 2026-04-22T15:20 |
| TASK-025-D | `projects/web-automation/src/tistory_post/post_saver.py` 신규. **함수**: `async def save_draft(page, payload: DraftPayload) -> int`. **구현**: `page.evaluate` 로 `fetch('/manage/drafts', {method:'POST', headers:{'Content-Type':'application/json'}, body: JSON.stringify(payload_dict)}).then(r=>r.json())` 호출. `payload.draftSequence` 가 None 이면 JSON 에서 제외(혹은 null 허용 — probe 로 검증). 응답 `success=true` 확인 후 `draft.sequence` 반환. HTTP 4xx/5xx → `RuntimeError(f"drafts API 실패: status={status}, body={body[:500]}")`. `success=false` → `RuntimeError(f"drafts API 거부: {body}")`. | coder | TODO | HIGH | TASK-025-A0 | 2026-04-22T15:20 | 2026-04-22T15:20 |
| TASK-025-E | `projects/web-automation/src/tistory_post/post_runner.py` 신규. **함수**: `async def run_post(page, folder: Path) -> RunResult`. **흐름**: (1) post_loader.load_post(folder) — skip=True 면 `RunResult(skipped=True, ...)` 즉시 반환. (2) category_fetcher.fetch_category_map(page). (3) image_uploader.upload_images(page, image_paths). (4) post_builder.build_payload(loaded, uploads, cat_map). (5) `.draft_id` 파일 읽어서 `payload.draftSequence` 주입. (6) post_saver.save_draft(page, payload) → sequence 획득. (7) `.draft_id` 에 sequence 저장. (8) `.published` 에 ISO 시각 기록. (9) 성공 시 기존 `.error`·`.orphan.log` 삭제 (이전 실패 흔적 제거 — M7). **에러 롤백 계약** (M7 반영): `PartialUploadError` catch → `.orphan.log` 에 JSONL append (각 줄: `{"ts": ISO, "key": ..., "url": ..., "filename": ...}`). `.error` 에 JSON 덮어쓰기: `{"traceback": str, "phase": "upload"\|"build"\|"save", "ts": ISO}`. 기타 예외도 phase 판별 후 `.error` 기록. retry 없이 1회만 — raise 후 사용자 재실행으로 위임. | coder | TODO | HIGH | TASK-025-A,TASK-025-B,TASK-025-CAT,TASK-025-C,TASK-025-D | 2026-04-22T15:20 | 2026-04-22T15:20 |
| TASK-025-F | **스모크 러너 + 샘플 폴더** (M8 반영). 산출물: (1) `projects/web-automation/scripts/smoke_tistory_post.sh` — `smoke_tistory_login.sh` 구조 재사용 (xvfb auto-detect, .env 4필드, PYTHONPATH, playwright chromium 체크). (2) `projects/web-automation/scripts/smoke_tistory_post.py` — login 후 `post_runner.run_post(folder)` 호출. (3) 샘플 폴더 `projects/web-automation/posts/2026-04-22-sample/` 신규 생성 — 파일: **post.md**: `---\ntitle: "[Phase 7 스모크] 폴더 기반 자동화 테스트"\ncategory: "메모"\ntags: ["스모크","자동화","phase7"]\n---\n\n# 본문 시작\n첫 문단에 이미지 하나 → ${1}\n\n**굵은 글씨** 와 *기울임* 테스트.\n\n- 리스트 항목 1\n- 리스트 항목 2\n\n두 번째 이미지는 파일명으로 지정 → ${hello.png}\n`. **image1.png**: 100x100 단색 파랑 PNG (Pillow 로 `Image.new('RGB',(100,100),'blue').save('image1.png')` 생성). **hello.png**: 100x100 단색 빨강 PNG. (4) `.gitignore` 에 `projects/web-automation/posts/*/` 추가 (샘플 제외 규약은 샘플 폴더를 `posts/sample-included/` 같은 약속으로 분리하거나 의도 커밋). | coder | TODO | HIGH | TASK-025-E | 2026-04-22T15:20 | 2026-04-22T15:20 |
| TASK-025-G | 사용자 육안 검증: `./scripts/smoke_tistory_post.sh` → 관리자 임시저장 모달에서 `[Phase 7 스모크] 폴더 기반 자동화 테스트` draft 열기 → 이미지 2개 배치·카테고리 메모·태그 3개 확인 → 실제 '완료' 클릭으로 발행까지 시도 (또는 발행 직전 preview). `.published` 마커 생성 확인, 재실행 시 skip 확인. | user | TODO | HIGH | TASK-025-F | 2026-04-22T15:20 | 2026-04-22T15:20 |
