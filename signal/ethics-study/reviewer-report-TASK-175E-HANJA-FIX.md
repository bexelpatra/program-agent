---
task_id: TASK-175E-HANJA-FIX
verdict: PASS
---

# Reviewer Report: TASK-175E-HANJA-FIX

## 검증 대상

Manager 가 `task-board.md` L196 에 등록한 태스크:

- **Title**: 2014-A, 2014-B, 2015-A coverage 3파일 한자+한글 병기 보강 (architecture.md 조항 4 소급 적용). 한자 단독 노출을 찾아 `한자(한글독음 — 간단 의미)` 형식으로 일괄 치환. 원문 인용 구절은 예외(원문 보존). 집계 섹션·메모·해설이 주 대상
- **Assignee**: coder(opus)
- **Depends On**: TASK-175E-2015-A-T (DONE/PASS/observation)
- **Status**: IN_PROGRESS (2026-04-20T22:05) ← **Reviewer PASS 전에 이미 상태가 변경됨**

## 검증 결과

### 파일 존재

3 파일 전수 실존 + 비어있지 않음 확인 (ls 결과):

| 파일 | 크기 | 줄 수 | 비고 |
|------|------|-------|------|
| `projects/ethics-study/exam-solutions/coverage/2014-A.md` | 27,725 B | 103 | 실존 |
| `projects/ethics-study/exam-solutions/coverage/2014-B.md` | 17,654 B | 97 | 실존 |
| `projects/ethics-study/exam-solutions/coverage/2015-A.md` | 38,479 B | 162 | 실존 |

### 한자 단독 노출 실태 (grep)

Tester report 가 지적한 `禮`·`化性`·`天人之分` 의 2015-A 실제 위치 grep 확인:

```
2015-A.md:18 (기입형 4 row 메모 컬럼 끝)
  "예(禮)·禮義·化性起僞·天人之分" 직접 claim 보강 권장
2015-A.md:36 (BLOCKER-1 HTML 주석 — grep 0 건 검증 대상 key-list)
  `禮`·`예의`·`化性`·`화성기위`·`천인분이` 모두 **원문 hit 0**
2015-A.md:59 (ES canonical 매핑 섹션)
  - 순자 (荀子) — `xunzi` (기입형 4; claim "천론·天人之分·성악·화성기위·예(禮)·禮義" 직접 claim 보강 권장)
2015-A.md:134,136 (grep 검증 표의 키워드 컬럼)
  | "禮" | 0 | ... |
  | "化性" | 0 | ... |
2015-A.md:152 (집계 섹션 '부족' 열거)
  - **부족**: 기입형 4, 6, 8 = **3** (xunzi 禮/化性 claim 보강, ...)
```

→ Tester 가 observation 으로 지적한 구체 사례(`禮`·`化性`·`天人之分`)가 2015-A.md 에 실제로 bare 또는 축약 형태로 존재함 **확인**. Manager 주장 배경 1번 정합.

2014-A.md 의 hanja 토큰 유니크 리스트 스캔 결과:

- 상당수는 `한글(漢字)` 또는 `漢字(한글)` 병기 형식으로 이미 처리됨 — 예: `공(空)`, `무(無)`, `無用之用(원문 바로 앞 한글 해설 포함)`, `중론(中論)`, `팔불중도(八不中道)`, `이(理)`.
- 단, 일부 구절에서 **한자 단독** 또는 **축약 재노출** 관찰:
  - L18 기입형 4 row 의 `理觀`, `理到`, `理發` (첫 노출이 단독 한자로만; 한글 병기 없음).
  - 동 L18 `大學`, `大學章句`, `誠意章`, `太學`, `圖` 은 한글 `대학` 등과 별개 토큰으로 단독 노출되나 같은 셀 본문에 한글 맥락은 있음. (엄밀 적용 시 observation).
  - L18 `神人合一`, `神人不材`, `神壇樹`, `神市`, `在世理化` 등 한자 단독/축약.
  - L21 `八不中道` 한글 병기는 `팔불중도(八不中道)` 형태로 이미 병기됨 — PASS.
- 결론: 2014-A 에도 한자 단독 또는 축약 재노출 패턴이 **존재**. Manager 주장(2014-A 소급 보강 필요) 정합.

2014-B.md hanja 토큰 스캔 결과:

- `[\x{4E00}-\x{9FFF}]+` 유니크 토큰 **0 건**. 2014-B 는 한자 노출이 전혀 없음 (논술형 2 사상가 3인[밀·칸트·흄]이 서양 사상가라 한자 부재).
- → **2014-B 는 본 태스크의 보강 대상이 아님**. Manager 가 2014-B 를 3파일 목록에 포함한 것은 **과잉 지정**.

### 원문 인용 예외 처리

Manager task-board row: `원문 인용 구절은 예외(원문 보존). 집계 섹션·메모·해설이 주 대상` 명시됨. **예외 규칙은 문자열에 존재**.

그러나 Coder 가 식별 가능한가 측면:

- 2014-A/2015-A 의 coverage 표 구조에서 "제시문 핵심(원문 복사)" 컬럼(col 4)은 원문 인용이고, "사상가/개념" 컬럼(col 5), "ES 커버리지" 컬럼(col 8)은 Coder 해설이다.
- 단, "발문 요지" 컬럼(col 3)도 Coder 가 요지를 썼지만 원문 발문 구절을 직접 인용할 수도 있어 **경계가 모호**함.
- 또한 2015-A.md L36 의 BLOCKER HTML 주석은 grep 키워드 식별자 성격이 강하므로(`` `禮` `` 같이 백틱 코드 포맷), Tester 도 이를 "grep 코드 식별자(예외 타당)"로 판정했다(tester-report L221).
- → Coder 에게 "컬럼별 예외 구분 가이드"가 task-board row 에 **부족**. 다음 구분이 명시되어야 함:
  1. `"..."` 안쪽 원문 복사 구절 → 보존
  2. `` `키워드` `` 백틱 코드/grep 식별자 → 보존 (또는 병기 선택)
  3. 기타 해설·판정 근거·집계·메모 → 한자+한글 병기 적용

### 선행 의존성

- `task-board.md` L195: `TASK-175E-2015-A-T | tester | DONE(PASS,observation) | HIGH | TASK-175E-2015-A` **확인**.
- `signal/ethics-study/tester-report-TASK-175E-2015-A-T.md` 실존 + frontmatter `verdict: PASS` / `severity: observation` **확인**.
- tester-report 본문 L216~L228 에서 2015-A 의 한자 축약 재노출을 구체 위치(L18, L152, L136)와 함께 observation 지적 **확인**.

Manager 주장 "배경 1" (Tester PASS/observation 후속으로 본 태스크 생성) 정합.

### 배치 규칙 적용 판단

architecture.md 조항 6 "1연도×1과목 배치" 규정(L562~L567):

> 기출 관련 Coder 호출 1회의 작업 단위는 **1개 연도 × 1개 과목(A 또는 B)**으로 고정한다. ... 한 호출에서 복수 연도·복수 과목·커버리지 맵 전체를 처리하는 태스크는 금지한다. 이는 TASK-175A에서 293 row 일괄 처리가 성실성 붕괴(2014~2019 구간 대량 오매핑)를 유발한 사례를 반영한 제약이다.

본 태스크는 coverage map **재작성**이 아니라 한자 병기 **치환 보강**이므로 "3파일 일괄 처리"가 합리적인가?

- 조항 6의 입법 취지는 "원문 직독을 요구하는 풀이·분류 작업이 대량으로 누적되면 성실성이 붕괴하여 오매핑이 발생한다"이다.
- 본 태스크는 ① 원문 재독해 불요, ② 이미 확정된 사상가·분류 변경 없음, ③ 순수 문자열 치환성 작업이다.
- 따라서 **조항 6 문언의 엄격 적용 대상은 아니**며 Manager 의 "3파일 일괄 처리 가능" 주장은 합리적.
- 다만 리스크: 3파일 치환 중 어느 파일에서 실수가 발생하면 3파일 모두 원복이 필요할 수 있음. 파일별 독립 diff 제출 요구(`각 파일 diff 단위로 Coder 가 기록`)를 추가하면 안전.

### 조항 4 실존

architecture.md L551~L556 에 **조항 4 "한자+한글 병기 원칙 (사용자 가독성)"** 실존 확인:

```
551: 4. **한자+한글 병기 원칙 (사용자 가독성)**
552:    - 본 프로젝트 산출물... 한자 개념어·인명·저서명·trademark 구절을 노출할 때는 반드시 `한자(한글독음 — 간단 의미)` 형식으로 **병기**한다.
553:    - 예: `禮義(예의 — 예와 의)`, `化性起僞(화성기위 — 본성을 교화하여 인위를 일으킴)`, `天人分異(천인분이 — 하늘과 사람의 직분을 구분함)`.
554:    - 기술의 주언어는 **한글 해석 용어**여야 한다.
555:    - 예외: 원문 인용구절 자체는 원문 그대로 복사(원문 보존 원칙).
556:    - Tester 는 한자 단독 노출(한글 병기 누락)을 발견하면 `severity: observation` 이상으로 지적한다.
```

→ 조항 4 실존 **확인**. 치환 형식(`한자(한글독음 — 간단 의미)`)이 architecture.md 예시와 일치.

## 판정

**NEEDS_REVISION**

사유:

1. **2014-B.md 는 대상 아님** — hanja 토큰 0 건. 3파일 목록에서 **2014-B 제외** 필요. 포함 유지 시 Coder 가 "보강 대상 없음" 이라는 공허 report 를 제출하거나, 불필요한 한자를 추가 삽입할 위험(주 언어가 한글이어야 한다는 조항 4 전제 위배).
2. **task-board row 에 치환 대상·예외 식별 가이드 부족** — Coder 가 "원문 인용 구절"과 "해설/메모"를 어느 컬럼·어느 포맷(따옴표·백틱)으로 구분할지 모호. 특히 coverage 표의 col 3(발문 요지) 처리 기준 부재.
3. **이미 IN_PROGRESS 로 상태 변경됨** — Reviewer PASS 이전에 task-board status 가 `IN_PROGRESS(2026-04-20T22:05)` 로 갱신되어 있음. CLAUDE.md Step 3 규칙상 Reviewer PASS 후 Coder 호출과 동시에 IN_PROGRESS 로 변경해야 함. 현재 Coder 가 호출된 상태인지 확인 필요 — 호출됐다면 본 Reviewer 가 호출 전에 개입하지 못했음을 보고.
4. **치환 구체 형식 예시가 task-board row 에 없음 (Manager 주장 요약 6번에만 존재)** — Manager 주장 요약은 Reviewer 에게만 전달되었고, Coder 가 받을 task-board row 에는 치환 형식(`禮義(예의 — 예와 의)`) 예시가 없음. task-board row 에 2~3개 구체 before/after 예시 추가 필요.

## 수정 요청 (NEEDS_REVISION 시)

Manager 가 task-board.md L196 의 TASK-175E-HANJA-FIX row 를 아래와 같이 수정할 것:

1. **대상 파일 2파일로 축소**: `2014-A.md, 2015-A.md` (2014-B 제외; grep 결과 한자 토큰 0 건).
2. **치환 대상/예외 구분 명시**:
   - 치환 대상: ① 표의 col 5(사상가/개념) / col 8(ES 커버리지) 컬럼의 한자 단독·축약 토큰, ② 집계 섹션(`## 분류 카운트`·`## ES 커버리지`·`### 사상가형 / 교과교육학 / 경계영역` 등)의 한자 축약, ③ 불릿 리스트의 한자 축약.
   - 예외(보존): ① `"..."` 따옴표 안쪽 원문 복사 구절, ② `` `...` `` 백틱 내 grep 키워드/코드 식별자, ③ HTML 주석 내 grep 키워드 나열, ④ L3 "원문" 파일 경로.
3. **치환 형식 예시 추가** (task-board row 본문에):
   - `禮` → `예(禮 — 예)`
   - `禮義` → `예의(禮義 — 예와 의)`
   - `化性起僞` → `화성기위(化性起僞 — 본성을 교화해 인위를 일으킴)`
   - `天人之分` → `천인지분(天人之分 — 하늘과 사람의 직분 구분)`
   - `化性` 축약 → `화성(化性)` (복원 병기)
   - `理到`/`理發` → `이도(理到)`/`이발(理發)`
4. **Coder 에게 diff 제출 요구** — Coder report 에 파일별 before/after diff 또는 치환 위치 리스트(L번호 + 원문 → 변경문) 명시.
5. **상태 되돌리기**: task-board L196 상태를 `IN_PROGRESS` → `TODO` 로 되돌리거나, Coder 가 이미 작업 중이면 본 Reviewer 수정 요청을 반영해 재지시.

## Manager에게 전달

- **검증 결과**: Manager 주장 6개 항목 중 5개(배경, 조항 4 신설·실존, 한자 단독 노출 실태, 선행 의존성, 배치 규칙 합리성) 는 정합. 단 **대상 파일 목록**(2014-B 포함)이 과잉이고, **task-board row 의 Coder 지시 명확성**이 부족.
- **권장 조치**: 위 "수정 요청" 1~5 를 반영하여 task-board L196 을 갱신한 뒤, 본 Reviewer 에게 재검증 요청하면 PASS 판정 가능.
- **추가 관찰**: 2015-A 의 bare hanja 축약 재노출은 Tester report L216~L228 기준 "집계/리스트 재참조" 성격이라 **판정 영향 없음(observation)**. 본 소급 보강은 완성도 향상 목적이므로 급하지 않음. 2015-B 로 바로 진행 후 후행 배치에서 일괄 처리하는 대안도 가능(Manager 판단).
- **Coder 호출 차단 요청**: task-board 가 이미 IN_PROGRESS 로 변경된 상태이므로, Manager 는 Coder 가 이미 호출됐는지 확인하고, 호출됐다면 본 Reviewer NEEDS_REVISION 을 즉시 전달해 재호출 또는 현 작업의 재지시로 전환 바람.

---

## 재검증 (2차) — 2026-04-20

Manager 가 1차 NEEDS_REVISION 지적을 반영해 `task-board.md` L196~L197 을 수정했다고 주장. 실제 파일 재확인.

### 1. task-board TASK-175E-HANJA-FIX row (L196) 확인

수정된 문자열 전수 검토 결과:

| 1차 지적 사항 | Manager 수정 반영 | 판정 |
|---|---|---|
| 대상 2파일로 축소 (2014-B 제외) | Title 모두 `"2014-A, 2015-A coverage **2파일**"` + `"2014-B는 한자 노출 0건으로 대상 제외(Reviewer 확인)"` 명시 | PASS |
| 치환 대상 영역 컬럼·패턴 단위 명시 | `"메모 컬럼의 해설 문장·판정 근거·분류 설명·집계 섹션·ES 커버리지 column 내 한글 서술"` (typo: `"columnn"` 3n 표기 — 경미함) | PASS |
| 예외(보존) 영역 구체 명시 | ① `"..."` 또는 `『...』` 원문 직접 인용, ② 백틱 `` `...` `` 코드/식별자/URL, ③ HTML 주석 `<!-- ... -->` 내 grep 키워드·BLOCKER 근거 — 3 범주 모두 명시 | PASS |
| 치환 형식 + before/after 예시 ≥2개 | 형식 `한자(한글독음 — 간단 의미)` + 예시 4개 (`禮 → 禮(예)`, `化性起僞 → 化性起僞(화성기위 — 본성을 교화하여 인위를 일으킴)`, `天人之分 → 天人之分(천인지분 — 하늘과 사람의 직분 구분)`, `理發 → 理發(이발 — 이가 발함)`) | PASS |
| Coder diff 제출 요구 | `"Coder report 에 row별 before/after diff 샘플 포함"` 명시 | PASS |
| 상태 IN_PROGRESS → TODO 롤백 | L196 Status 칸 `TODO` 확인 (이전 `IN_PROGRESS(2026-04-20T22:05)` 에서 복원됨) | PASS |

### 2. task-board TASK-175E-HANJA-FIX-T row (L197) 확인

L197 Title: `"한자 병기 보강 2파일 전수 검증: ① 치환 대상 영역의 한자 단독 노출 0건 확인, ② 원문 인용/백틱/HTML 주석 영역 비변경 확인, ③ 기존 row 매핑(사상가·분류·ES 커버리지·집계 카운트) 비변경 확인"` — 3 검증 기준 모두 명시. Status: `TODO`. 의존성: `TASK-175E-HANJA-FIX`. **PASS**

### 3. 이전 PASS 항목 유지 확인

- ✅ `architecture.md` L551~L556 조항 4 "한자+한글 병기 원칙" 실존 (재확인).
- ✅ 선행 의존성 `TASK-175E-2015-A-T` 상태 `DONE(PASS,observation)` (L195).
- ✅ 대상 2파일 실존: `2014-A.md` (27,725B), `2015-A.md` (38,479B) — `ls` 확인. 2014-B.md 는 별도 존재하나 대상 제외.
- ✅ 한자 단독 노출 실재 (1차 grep 결과 유지: 2015-A.md L18, L59, L134, L136, L152 / 2014-A.md L18).

### 4. Coder 독립 실행 가능성 판정

Coder 가 task-board row 만 읽고 다음을 결정할 수 있는가:
- **어느 파일을 건드릴지**: 2014-A.md, 2015-A.md (명확).
- **어느 영역을 치환할지**: 메모 컬럼 해설·판정 근거·분류 설명·집계 섹션·ES 커버리지 컬럼 한글 서술 (명확).
- **어느 영역을 보존할지**: `"..."`/`『...』` 인용, 백틱 코드, HTML 주석 (명확 — 마크다운·문법 차원의 식별 가능 영역).
- **어떤 형식으로 치환할지**: `한자(한글독음 — 간단 의미)` + 4개 구체 예시 (명확).
- **무엇을 보고서에 포함할지**: row별 before/after diff 샘플 (명확).

→ Coder 가 추가 질의 없이 독립 실행 가능한 수준. **PASS**.

### 추가 관찰 (판정 영향 없음)

- 예시의 치환 방향: Manager 는 `한자(한글독음 — 의미)` 형식(한자-first)을 채택했다. 이는 architecture.md L553 의 예시(`禮義(예의 — 예와 의)`)와 **일치**한다. 본 Reviewer 의 1차 수정 요청(L134~L139)에서 한글-first (`예(禮 — 예)`) 예시를 제시했으나, architecture.md 의 공식 예시와 합치하는 한자-first 를 Manager 가 채택한 것은 **타당**하며 일관성 측면에서 오히려 우수.
- 경미한 typo: L196 `"ES 커버리지 columnn"` — 3n 표기. 의미 전달에는 영향 없고 Coder 가 해석 가능. 굳이 수정할 필요 없음.
- `禮 → 禮(예)` 예시는 1글자 한자이므로 `"— 간단 의미"` 부분이 생략된 약식 형태. architecture.md L552 에서도 `理發` 급 단문은 한글 독음만 병기하는 것이 자연스러우므로 허용. Coder 가 긴 개념어에는 `— 의미` 덧붙이고 1~2자 약식 한자는 `(한글독음)` 만 적용하는 판단이 가능하도록 예시가 양쪽을 모두 제시함.

### 판정

**PASS**

### Manager에게 전달

- **검증 결과**: 1차 NEEDS_REVISION 6개 지적 사항 모두 반영 완료. task-board L196 (TASK-175E-HANJA-FIX) / L197 (TASK-175E-HANJA-FIX-T) row 모두 Coder/Tester 독립 실행 가능한 수준으로 구체화됨. 이전 PASS 항목(조항 4 실존, 선행 의존성 DONE, 파일 실존·한자 노출 실재)도 유지 확인.
- **권장 조치**: Coder(opus) 를 호출하여 TASK-175E-HANJA-FIX 진행. 호출 시점에 task-board L196 상태를 `TODO` → `IN_PROGRESS` 로 변경(CLAUDE.md Step 3.1 규칙).
- **후속 Reviewer 재호출 불요**: 본 태스크는 Coder 호출 직전 단계까지 Manager 산출물 검증이 완료됨. 이후 Coder 산출물 품질은 Tester(TASK-175E-HANJA-FIX-T) 가 검증.
- **경미한 개선 여지 (선택)**: row 본문의 typo `"columnn"` → `"column"` 수정 가능. 실행에 영향 없으므로 Manager 판단으로 생략 가능.
