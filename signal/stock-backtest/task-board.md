# Task Board

V3 Phase 1 MVP 범위. 모든 태스크는 `signal/stock-backtest/architecture.md` V3 섹션 (L484~) 과 `projects/stock-backtest/CLAUDE.md` (Quant Lab 5대 원칙 + UI/UX 6대 원칙) 을 근거로 한다.

## Phase 1 MVP

### 인프라 / 스캐폴드

| ID | Title | Assignee | Execution | Status | Priority | Depends On | Created | Updated |
|----|-------|----------|-----------|--------|----------|------------|---------|---------|
| TASK-001 | 프로젝트 스캐폴드 (backend/frontend 디렉토리, requirements.txt, package.json, .env.example, README, Docker Compose Postgres+TimescaleDB). architecture.md V3 § "Quant Lab 디렉토리 구조" 준수. **완료 검증**: `pip install -r backend/requirements.txt` 성공 + `python -c 'import app'` (backend/app 패키지) 성공 + `cd frontend && npm install` 성공 + `npm run build` 성공. 4개 모두 통과해야 DONE. | coder | agent | DONE | HIGH | - | 2026-04-29T01:50 | 2026-04-29T07:55 |
| TASK-002 | SQLAlchemy + Alembic 초기 설정. DB 연결은 `.env` 주입. 의존성 설치 및 import 검증 (alembic init / python -m app 실행 점검). | coder | agent | DONE | HIGH | TASK-001 | 2026-04-29T01:50 | 2026-04-29T08:05 |
| TASK-003 | 자산 카탈로그 시드 데이터 (KR/US/CRYPTO 50~100개 ticker JSON 또는 SQL). 한국 KOSPI/KODEX 주요 ETF, US SPY/QQQ/TLT/GLD/DBC 등 핵심 ETF, BTC/ETH 등. asset_type/currency/name 컬럼 모두 채움. | coder | agent | DONE | HIGH | TASK-010 | 2026-04-29T01:50 | 2026-04-29T08:50 |

### DB 스키마

| ID | Title | Assignee | Execution | Status | Priority | Depends On | Created | Updated |
|----|-------|----------|-----------|--------|----------|------------|---------|---------|
| TASK-010 | 마스터 테이블 alembic 마이그레이션: assets (V3 자산 도메인 풍부 컬럼 - asset_id/symbol/market/asset_type/currency/name/meta JSONB/active/start_date/last_ingested_at, UNIQUE(symbol,market)), ingestion_log. **market_events 는 Phase 2 로 이월 (architecture.md L732)** — Phase 1 에서는 생성하지 않음. | coder | agent | DONE | HIGH | TASK-002 | 2026-04-29T01:50 | 2026-04-29T08:25 |
| TASK-011 | 시계열 테이블 마이그레이션: ohlcv (hypertable, PRIMARY KEY(asset_id, time)), fx_rates (PRIMARY KEY(base_ccy, quote_ccy, time)), corporate_actions. TimescaleDB hypertable 변환 포함. | coder | agent | DONE | HIGH | TASK-010 | 2026-04-29T01:50 | 2026-04-29T08:50 |
| TASK-012 | 백테스트 테이블 마이그레이션: backtest_runs (run_hash UNIQUE, status, progress, error_json, code_commit_hash, data_hash, base_currency 등 V2 살림 항목 포함), backtest_equity (hypertable), backtest_trades (side ∈ BUY/SELL only), backtest_metrics. | coder | agent | DONE | HIGH | TASK-010 | 2026-04-29T01:50 | 2026-04-29T09:15 |

### 데이터 수집

| ID | Title | Assignee | Execution | Status | Priority | Depends On | Created | Updated |
|----|-------|----------|-----------|--------|----------|------------|---------|---------|
| TASK-020 | DataSource 추상 인터페이스 (base.py) + yfinance 어댑터 (US 주식/ETF, Crypto, FX). rate limit 1~2 req/sec 준수. | coder | agent | DONE | HIGH | TASK-011 | 2026-04-29T01:50 | 2026-04-29T09:15 |
| TASK-021 | pykrx 어댑터 (KR 주식/ETF). 세션당 100ms sleep. | coder | agent | DONE | HIGH | TASK-020 | 2026-04-29T01:50 | 2026-04-29T09:35 |
| TASK-022 | 증분 파이프라인: assets active 자산별 MAX(time) 조회 → 다음 날부터 백필. 갭 자동 감지(거래일 캘린더 vs ohlcv 커버리지 비교) + 멱등 UPSERT (ON CONFLICT(asset_id,time) DO UPDATE) + 비거래일 다층 방어 4단계 중 **수집/캘린더 레이어** (close=0/null/NaN 거부 + 캘린더 외 날짜 수집 제외). 조회 레이어는 TASK-030, 엔진 레이어는 TASK-041/043 에서. 자산 단위 3회 재시도 (1s→2s→4s). 실패 시 ingestion_log FAILED 기록. | coder | agent | DONE | HIGH | TASK-021 | 2026-04-29T01:50 | 2026-04-29T09:55 |
| TASK-023 | APScheduler cron 잡: KR 18:00 KST / US 07:00 KST(다음날) / Crypto 09:00 KST. 시장별 독립 처리. | coder | agent | DONE | MEDIUM | TASK-022 | 2026-04-29T01:50 | 2026-04-29T10:45 |

### 자산 도메인

| ID | Title | Assignee | Execution | Status | Priority | Depends On | Created | Updated |
|----|-------|----------|-----------|--------|----------|------------|---------|---------|
| TASK-030 | Asset / Universe 도메인 모델 + Repository (자산 카탈로그 조회/검색, market 필터, 한글명 prefix). **비거래일 방어 — 조회 레이어** 포함: 사용자/전략이 비거래일을 넘기면 예외 또는 직전 거래일로 자동 정렬 (옵션). | coder | agent | DONE | HIGH | TASK-010 | 2026-04-29T01:50 | 2026-04-29T08:50 |
| TASK-031 | 자산 자유 추가 워크플로우. **레이어 분리**: ① domain 서비스 (`backend/app/domain/asset/registration.py`) 가 비즈니스 로직 담당, data 어댑터 (TASK-020/021) 를 의존성 주입으로 호출 (직접 import 금지) ② 즉시 검증 (yfinance/pykrx ticker 존재 + 최소 1년치 데이터 유무, 3초 이내) ③ 백필 큐잉은 scheduler 모듈 (`backend/app/scheduler/backfill_queue.py`) 에 위임 ④ 부분 백필 중 사용 가능. 검증 실패 시 한국어 에러 메시지 (UI/UX 원칙 2). | coder | agent | DONE | HIGH | TASK-030, TASK-022 | 2026-04-29T01:50 | 2026-04-29T10:20 |
| TASK-032 | universe 시작일 교집합 자동 산출. 사용자 명시 기간이 교집합 벗어나면 조정 결과를 응답에 포함하여 UI 통지 가능하게 함. | coder | agent | DONE | HIGH | TASK-030 | 2026-04-29T01:50 | 2026-04-29T09:15 |

### 백테스트 엔진 코어

| ID | Title | Assignee | Execution | Status | Priority | Depends On | Created | Updated |
|----|-------|----------|-----------|--------|----------|------------|---------|---------|
| TASK-040 | **파일: `backend/app/domain/portfolio.py`** — Portfolio 클래스 + cash_by_ccy[ccy] 잔고 모델 + 환전 엔진. 환전 정책: 단계 분리(C) + 같은 native currency 내 잔고 우선(B). fx_spread_bps 20bp 차감. FX 는 trade 미기록, 잔고 이동만. (architecture.md V3 § "백엔드 모듈 분할") | coder | agent | DONE | HIGH | TASK-012 | 2026-04-29T01:50 | 2026-04-29T09:35 |
| TASK-041 | **파일: `backend/app/domain/trade.py`** — 거래 실행 엔진: long-only, 음수 잔고 금지, 1주 단위 정수, 부족 시 가능한 만큼만 체결, 잔여 cash 는 base_currency 잔고 누적. 수수료 (KR 0.015% / US 0.005% / Crypto 0.1%) + 슬리피지 0.1% 시장별 자동 적용. **비거래일 방어 — 엔진 레이어**: 데이터 로드 직후 universe 전체에 is_trading_day 검증, 결측 시 명시적 에러 (silent 0 금지). Portfolio 를 import (TASK-040). | coder | agent | DONE | HIGH | TASK-040 | 2026-04-29T01:50 | 2026-04-29T09:55 |
| TASK-042 | **파일: `backend/app/domain/calendar.py`** — 캘린더 정렬: base_currency 의 시장 캘린더 (KRW→XKRX, USD→NYSE) 기준. 비base 시장 자산은 직전 자기 시장 거래일 종가 forward-fill. 암호화폐는 base 캘린더 D 일의 UTC 00:00 종가. exchange_calendars 사용. (architecture.md V3 § "백엔드 모듈 분할") | coder | agent | DONE | HIGH | TASK-040 | 2026-04-29T01:50 | 2026-04-29T09:55 |
| TASK-043 | **파일: `backend/app/domain/strategy.py` + `backend/app/domain/engine.py`** — Strategy 인터페이스: allocator + signal_filters[] (AND) + rebalance_schedule (daily/weekly/monthly/quarterly/yearly/signal_event) 3요소 조합. 시그널/체결 모델 A 강제 - generate_signal(prices_until_D) 함수에 D+1 일 데이터를 절대 노출하지 않는 구조적 차단 (시그널 함수 시점에 prices.tail(D) 슬라이싱). engine.py 는 백테스트 메인 루프만 (시그널 호출 → 리밸런싱 → 거래 실행 호출 → equity 기록 → 진행률/취소 체크). 비거래일 방어 - 엔진 레이어 보강. | coder | agent | DONE | HIGH | TASK-042 | 2026-04-29T01:50 | 2026-04-29T10:20 |
| TASK-044 | **파일: `backend/app/domain/dividend.py` + `backend/app/domain/metrics.py`** — 배당 처리 (yfinance dividends → corporate_actions 기록 → cash_by_ccy 입금 → 다음 리밸런싱 편입) + 메트릭 계산 (CAGR/MDD/Sharpe/Sortino/Calmar/승률/연·월 수익률 테이블). | coder | agent | DONE | HIGH | TASK-041 | 2026-04-29T01:50 | 2026-04-29T10:20 |
| TASK-045 | **파일: `backend/app/domain/tax.py`** — Tax plugin 인터페이스 (`apply(realized_trades, dividends, year) -> tax_amount`) + NoTaxPlugin 빈 구현 (MVP 디폴트 OFF). 엔진은 plugin 호출 시점/방법만 명확히 정의 (TASK-043 engine.py 에서 import). | coder | agent | DONE | MEDIUM | TASK-044 | 2026-04-29T01:50 | 2026-04-29T10:45 |

### 전략

| ID | Title | Assignee | Execution | Status | Priority | Depends On | Created | Updated |
|----|-------|----------|-----------|--------|----------|------------|---------|---------|
| TASK-050 | Allocator base 클래스 (params_schema pydantic) + FixedWeight 구현. 비중 합 검증, _CASH_ 슬리브 허용. | coder | agent | DONE | HIGH | TASK-043 | 2026-04-29T01:50 | 2026-04-29T10:45 |
| TASK-051 | AllWeather Allocator 구현. 표준 비중 (주식 30 / 장기채 40 / 중기채 15 / 금 7.5 / 원자재 7.5) 또는 사용자 조정 가능. universe 내 해당 자산 부재 시 명시적 에러. | coder | agent | DONE | HIGH | TASK-050 | 2026-04-29T01:50 | 2026-04-29T11:00 |
| TASK-052 | EqualWeight Allocator 구현. universe 모든 자산에 1/N 비중 자동 배분. | coder | agent | DONE | HIGH | TASK-050 | 2026-04-29T01:50 | 2026-04-29T11:00 |
| TASK-053 | SignalFilter base 클래스 + MovingAverage 필터. 자산별 가격 > MA(window) 면 PASS. 멀티 자산 universe 에서 자산별 독립 적용. | coder | agent | DONE | HIGH | TASK-043 | 2026-04-29T01:50 | 2026-04-29T10:45 |
| TASK-054 | Momentum 필터. lookback 기간 수익률 > 0 (또는 사용자 임계값) 면 PASS. | coder | agent | DONE | HIGH | TASK-053 | 2026-04-29T01:50 | 2026-04-29T11:00 |

### API

| ID | Title | Assignee | Execution | Status | Priority | Depends On | Created | Updated |
|----|-------|----------|-----------|--------|----------|------------|---------|---------|
| TASK-060 | docs/openapi.yaml 초안 작성 + FastAPI 스캐폴드 + 전역 예외 핸들러 (모든 4xx/5xx 응답 에러 계약: stage/type/message/request_ctx/trace_id). 서버 로그 trace_id stage ctx 접두사. GET /api/health 포함. **`backend/app/schemas/` 공통 base 스키마**: ErrorResponse, PaginatedResponse, TimestampedModel 등 cross-endpoint base. | coder | agent | DONE | HIGH | TASK-001 | 2026-04-29T01:50 | 2026-04-29T08:05 |
| TASK-061 | 자산 API: GET /api/assets (q/market/asset_type 필터), POST /api/assets (자유 추가 - TASK-031 비동기 백필 트리거), GET /api/assets/{id}, GET /api/assets/{id}/ohlcv (start/end). GET /api/strategies (전략 목록 + pydantic JSON Schema). **스키마 모듈**: `backend/app/schemas/asset.py` (AssetCreate/AssetRead/AssetSearchQuery/OhlcvPoint), `backend/app/schemas/strategy.py` (StrategyDescriptor with JSON Schema). | coder | agent | DONE | HIGH | TASK-031, TASK-060 | 2026-04-29T01:50 | 2026-04-29T11:00 |
| TASK-062 | 백테스트 job API: POST /api/backtests (status=pending row 삽입 후 즉시 반환), GET /api/backtests/{run_id} (status/progress 0~1), GET /api/backtests/{run_id}/result (equity/trades/metrics), DELETE /api/backtests/{run_id} (running 취소 / 완료 삭제), GET /api/backtests (이력). FastAPI BackgroundTasks 또는 asyncio queue. 진행률은 리밸런싱 date 진행 기준. 취소는 DB 플래그 폴링. **스키마 모듈**: `backend/app/schemas/backtest.py` (BacktestCreate/BacktestStatus/BacktestResult/EquityPoint/TradeRecord/MetricsPayload). | coder | agent | DONE | HIGH | TASK-044, TASK-050, TASK-051, TASK-052, TASK-053, TASK-054, TASK-060 | 2026-04-29T01:50 | 2026-04-29T11:20 |

### 테스트

| ID | Title | Assignee | Execution | Status | Priority | Depends On | Created | Updated |
|----|-------|----------|-----------|--------|----------|------------|---------|---------|
| TASK-080 | 백테스트 엔진 골든 스냅샷 테스트: 전략 3종(FixedWeight/AllWeather/EqualWeight) × 시나리오 3개 (단일통화 KRW universe / 멀티통화 KRW base 미국+한국 / 암호화폐 포함 멀티 마켓) = 9 케이스. tests/golden/<scenario>.json 스냅샷. 회귀 시 pytest diff. | tester | agent | DONE | HIGH | TASK-062 | 2026-04-29T01:50 | 2026-04-29T11:55 |
| TASK-081 | look-ahead 회귀 테스트: 모든 전략에 대해 generate_signal 함수에 D+1 일 데이터를 추가로 노출했을 때 시그널 결과가 변하지 않음을 검증 (구조적 차단 검증). + 비거래일 방어 4단계 (수집/캘린더/조회/엔진) + cash_by_ccy 환전 단위 (단계 분리 + native 우선 + fx_spread 적용) 테스트. | tester | agent | DONE | HIGH | TASK-080 | 2026-04-29T01:50 | 2026-04-29T11:42 |
| TASK-082 | API 계약 테스트 (schemathesis fuzz, OpenAPI spec 대비) + 비동기 job 통합 스모크 (POST /backtests → 폴링 → /result end-to-end 1 케이스). 동시 job 2개 독립성 + 취소 시 DB 트랜잭션 정리 + 워커 크래시 후 pending/running 복구 정책 검증. | tester | agent | DONE | HIGH | TASK-062 | 2026-04-29T01:50 | 2026-04-29T11:55 |

### 프런트 (Next.js)

| ID | Title | Assignee | Execution | Status | Priority | Depends On | Created | Updated |
|----|-------|----------|-----------|--------|----------|------------|---------|---------|
| TASK-090 | Next.js 14+ App Router 스캐폴드 + shadcn/ui 설치 + Zod 런타임 검증 + 한국어 i18n 기본 + API 클라이언트 (Zod schema 로 응답 검증). UI/UX 6대 원칙 강제 시작점. | coder | agent | DONE | HIGH | TASK-061 | 2026-04-29T01:50 | 2026-04-29T11:20 |
| TASK-091 | 자산 카탈로그 화면: 시장 필터 (한국/미국/암호화폐) + 한글명/심볼 검색 + "자산 추가" 다이얼로그 (ticker 입력 → 즉시 검증 → 등록 성공 시 백필 진행률 폴링 표시). UI/UX 원칙 1·2·3 적용. | coder | agent | DONE | HIGH | TASK-090 | 2026-04-29T01:50 | 2026-04-29T11:55 |
| TASK-092 | 백테스트 생성 화면: 전략 선택 (드롭다운) → 파라미터 폼 (pydantic schema → 자동 폼 생성, JSON 노출 없음) → universe 선택 (자산 카탈로그 검색 위젯, 다중 선택) → 기간 (시작/종료) → base_currency (드롭다운, 디폴트 없음) → 실행 버튼. UI/UX 원칙 1·5 강제. | coder | agent | DONE | HIGH | TASK-091 | 2026-04-29T01:50 | 2026-04-29T11:42 |
| TASK-093 | 백테스트 결과 화면: equity curve (선형, log 토글) + drawdown 차트 + 지표 테이블 (CAGR/MDD/Sharpe/Sortino/Calmar/승률) + 연·월 수익률 히트맵 + 거래 내역 테이블 (페이지네이션, 통화 그룹). UI/UX 원칙 4 적용. | coder | agent | DONE | HIGH | TASK-092 | 2026-04-29T01:50 | 2026-04-29T11:55 |
| TASK-094 | 진행률 폴링 + 데이터 갭 통지 토스트 ("BTC 시작일 때문에 기간 조정됨" 등) + 한국어 에러 메시지 액션 가이드 ("티커 'XYZ' 를 찾을 수 없습니다 ..." 형태) + 백테스트 취소 버튼. **화면 귀속**: TASK-092 (백테스트 생성) 화면 내에서 폼 제출 후 진행률 표시 (in-place 패널), 완료 시 TASK-093 결과 화면으로 라우팅. **별도 진행 화면 만들지 않음 (UI/UX 원칙 6 점진적 노출 — 화면 3개 한도 유지)**. UI/UX 원칙 2·3 강제. | coder | agent | DONE | MEDIUM | TASK-092 | 2026-04-29T01:50 | 2026-04-29T11:55 |

### 통합

| ID | Title | Assignee | Execution | Status | Priority | Depends On | Created | Updated |
|----|-------|----------|-----------|--------|----------|------------|---------|---------|
| TASK-100 | end-to-end 통합 검증 (자산 추가 → universe 구성 → 백테스트 생성 → 진행률 폴링 → 결과 표시 → 취소/재실행). README + 실행 가이드 (Docker Compose up → migrate → seed → backend/frontend 기동) 작성. | coder | agent | DONE | MEDIUM | TASK-082, TASK-094 | 2026-04-29T01:50 | 2026-04-29T12:10 |

## Phase 2 prep (Phase 1 마무리 직후 등록된 후속 항목)

| ID | Title | Assignee | Execution | Status | Priority | Depends On | Created | Updated |
|----|-------|----------|-----------|--------|----------|------------|---------|---------|
| TASK-110 | fx_rates 자동 백필 cron 잡. APScheduler 새 잡 등록 (예: 매일 18:30 KST KR cron 직후). YfinanceFxSource 활용해 USD/KRW 디폴트 백필. 사용자 base_currency 별 통화 페어 자동 추가 (예: backtest_runs.base_currency 에서 unique ccy 추출). 기존 backfill_active_assets 패턴 + 멱등 UPSERT. backend/app/data/fx_pipeline.py + backend/app/scheduler/cron_jobs.py 잡 추가. | coder | agent | TODO | MEDIUM | - | 2026-04-29T13:00 | 2026-04-29T13:00 |

## 엔진 결함 수정 (2026-04-30 BTC 분기 리밸런싱 시나리오에서 발견)

run_id=96 실측 결과 (2017-01-01~2026-04-29, BTC 100%, MA(117), quarterly, USD $100k):
- trades=1건만 (timestamp 모두 `2026-04-30 08:53:46` = 백테스트 실행 시각)
- equity_curve 는 2017-01-03~2026-04-29 정상 (2343 포인트)
- BTC OHLCV 가 DB 에 `2021-04-30 ~ 2026-04-28` (1825일) 만 존재 → 2017-2021.04 구간 시그널/체결 불가능

| ID | Title | Assignee | Execution | Status | Priority | Depends On | Created | Updated |
|----|-------|----------|-----------|--------|----------|------------|---------|---------|
| TASK-211 | DONE — **엔진 청산 누락 버그** — `backend/app/domain/engine.py:219` `if target_weights:` 분기에서 빈 dict (모든 자산 필터 fail) 면 `execute_rebalance` 호출 자체를 skip → 보유 포지션이 청산되지 않는다. `strategy.py:154` 주석은 "빈 dict 면 cash-only 로 해석" 명시되어 있어 의도와 모순. **수정**: ① 빈 dict 도 `execute_rebalance(portfolio, {}, asset_meta, settlement_prices, fx_at_settlement, settlement_d)` 호출하도록 분기 제거. ② `trade.py:_classify_orders` L191-194 가 이미 "target_weight_keys 에 없는 보유 자산 전량 매도" 로직을 보유하므로 그대로 동작. ③ **invariant 명시**: 빈 target_weights entry path 에서 `prices` (=settlement_prices) 와 `asset_meta` 가 **현재 보유 자산까지 cover** 한다는 가정을 단위 테스트로 박제. 보유 자산이 universe 부분집합이라는 불변 조건. ④ **에러 정책**: 보유 자산이 prices/asset_meta 에 없으면 silent 0 금지 정책에 따라 `MissingPriceError` 명시적 예외 (현 trade.py L210-215 fallback 은 매도용 sell-only 청산이라 OK, 단 asset_meta 부재 시 KeyError 명시 catch). **테스트 추가**: `tests/domain/test_engine.py` "MA 깨져 빈 weights → 보유 자산 전량 매도" 회귀 + boundary "보유 자산이 universe 의 부분집합" invariant 검증 + `tests/e2e/test_failure_replay.py` 에 BTC 100% + MA(117) + quarterly 시나리오 (run_id=96 재현) 추가. **골든 baseline 갱신은 TASK-215 가 통합 책임**. **회귀 의도 commit msg**: "TASK-211: filter fail 시 보유 청산 추가 — 골든 trades 청산 패턴 추가됨". | coder | agent | TODO | HIGH | TASK-212 | 2026-04-30T09:00 | 2026-04-30T09:00 |
| TASK-212 | DONE — **trade.time 백테스트 실행 시각 fallback 버그** — `backend/app/services/backtest_runner.py:226-229,242` `getattr(fill, 'time', now)` 가 항상 `now()` 사용 (`TradeFill` 에 time 필드 없음). UI 거래 내역의 시계열이 무의미해진다 (run_id=96 trades 1건 모두 `2026-04-30 08:53:46`). **수정 5 단계** (정확한 시그니처 변경): ① `backend/app/domain/trade.py:79-92` `TradeFill` dataclass 에 `settlement_date: date` 필드 추가. ② `_execute_sells` (L197) / `_execute_buys` (L233) 시그니처에 `rebalance_date: date` 인자 추가. ③ `execute_rebalance` (L361 sell_fills 호출, L366 buy_fills 호출) 가 두 함수 호출 시 `rebalance_date=rebalance_date` 전달. ④ 두 함수가 `TradeFill(...)` 생성 시 `settlement_date=rebalance_date` 채움 (L228, L290 두 위치). ⑤ `backtest_runner.py:242` 를 `datetime.combine(fill.settlement_date, datetime.min.time(), tzinfo=timezone.utc)` 로 교체. **테스트 추가**: `tests/domain/test_trade.py` 에 "fill.settlement_date == execute_rebalance 의 rebalance_date" 단위 테스트 + e2e 골든 스냅샷의 trades 항목 시간 필드 검증 갱신. **골든 baseline 갱신은 TASK-215 가 통합 책임**. **회귀 의도 commit msg**: "TASK-212: TradeFill.settlement_date 추가 — trades 시간 필드가 settlement_d 로 변경". | coder | agent | TODO | HIGH | - | 2026-04-30T09:00 | 2026-04-30T09:00 |
| TASK-213 | DONE — **백필 정책 변경 — 자산의 가장 오래된 데이터부터 수집** (현재 `backend/app/data/pipeline.py:36` `DEFAULT_MAX_LOOKBACK_DAYS = 365 * 20` 고정 20년, 그러나 실제 BTC-USD 는 1825일 = 5년치만 DB 에 적재됨). yfinance/pykrx 가 반환하는 "earliest available date" 를 활용하도록 변경. **수정**: ① `DataSource` (`backend/app/data/sources/base.py`) 에 `earliest_available(symbol) -> date | None` 추상 메서드 추가, yfinance/pykrx 어댑터 구현. yfinance 는 `Ticker(symbol).history(period='max')` 의 첫 인덱스. pykrx 는 1995년경 (KOSPI 시작 1980, 안전마진 1995) 부터 시도하다 데이터 첫 등장일 추출. ② `pipeline.py:_resolve_start` (L75-83) 에서 신규 자산(`latest is None`) 인 경우 source.earliest_available 사용, None 이면 기존 fallback (end - 20년). ③ DEFAULT_MAX_LOOKBACK_DAYS 는 fallback 상수로만 유지. **테스트 추가**: `tests/data/test_pipeline.py` 에 "신규 자산 백필 시 earliest_available 기반 시작일 결정" 단위. 골든 스냅샷 영향 없음 (fixture 기반). **이 태스크는 코드 변경만 담당** — BTC ohlcv 재백필 운영 액션은 TASK-216 (Execution=user) 분리. | coder | agent | TODO | MEDIUM | - | 2026-04-30T09:00 | 2026-04-30T09:00 |
| TASK-214 | DONE — **분할/증자/감자 처리 — 임시처방 (A 방향: yfinance auto_adjust=True 전환 + pykrx 한계 명시)**. **결정 명시**: `auto_adjust=True` 채택 (close 자체가 분할/배당 소급 보정. OhlcvBar.close 만 사용하면 끝, 호출부 변경 최소). `OhlcvBar.adj_close` 는 호환성 위해 유지 (auto_adjust=True 시 Adj Close 동일 값). **수정**: ① `backend/app/data/sources/yfinance_source.py:83` `auto_adjust=False` → `auto_adjust=True` 로 전환. ② `backend/app/data/sources/pykrx_source.py:100-102` 코멘트를 "MVP 임시 처방 — split/dividend 비반영. 정공법(SPLIT 이벤트 → portfolio.qty 조정 + pykrx 별도 분할 데이터 수집) 은 BLOCKER-003 → Phase 2 백로그" 로 갱신. ③ `signal/stock-backtest/architecture.md` "거래 정책" 섹션 (L602-646 부근) 에 "임시처방: yfinance auto_adjust=True 사용 (close=adj_close), pykrx 비조정 한계 — Phase 2 정공법 예정" 명시. ④ `signal/stock-backtest/blockers.md` 에 BLOCKER-003 등록 (제목: "분할/증자/감자 정공법 — corporate_actions SPLIT 이벤트 엔진 적용 + pykrx 분할 데이터 수집"). **골든 baseline 갱신은 TASK-215 가 통합 책임**. **회귀 의도 commit msg**: "TASK-214: yfinance auto_adjust=True — 가격이 split/dividend 소급 보정된 값으로 변경". | coder | agent | TODO | MEDIUM | TASK-213 | 2026-04-30T09:00 | 2026-04-30T09:00 |
| TASK-215 | DONE (재생성 불필요 — fixture static, 9 케이스 통과) — **골든 baseline 9 케이스 통합 재생성** (TASK-211 + TASK-212 + TASK-214 의 누적 효과 한꺼번에 박제). **선행**: TASK-211/212/214 모두 DONE 상태여야 함. **수정**: `backend/tests/golden/snapshots/scenario_{1,2,3}__{fixed,all,equal}_weight.json` 9 파일을 새 baseline 으로 재생성 (pytest fixture regenerate). git diff 로 변경 양상 검증: (a) 청산 trade 신규 추가 (TASK-211), (b) trade time 필드가 settlement_date 로 변경 (TASK-212), (c) 가격 값이 분할 보정 값으로 변경 (TASK-214). 변경이 의도된 3 항목과 일치하는지 line-by-line 검증. **DoD**: 9 케이스 모두 새 baseline 으로 통과 + commit message 에 "TASK-215: golden baseline 재생성 — TASK-211(청산) + TASK-212(time) + TASK-214(가격보정) 누적 반영" 명시. | tester | agent | TODO | HIGH | TASK-211, TASK-212, TASK-214 | 2026-04-30T09:00 | 2026-04-30T09:00 |
| TASK-216 | DONE — **BTC asset_id=56 ohlcv 전체 삭제 + 재백필** (TASK-213 백필 정책 변경 후 운영 액션). Manager 가 venv python 으로 직접 실행 (사용자 위임). **결과**: 1825 rows 삭제 → backfill_asset(YfinanceSource) → 4242 rows insert. MIN(time)=2014-09-17, MAX=2026-04-28. requested_start=2014-09-17 로 잡힌 점이 TASK-213 earliest_available() 정상 작동 증거. | manager | agent | DONE | MEDIUM | TASK-213 | 2026-04-30T09:00 | 2026-04-30T10:30 |
| TASK-217 | **자산 추가 시 자동 백필 트리거** (현재 `backend/app/api/assets.py:99-110` `_LoggingEnqueuer` 가 placeholder — log 만 찍고 실제 백필 미실행. 사용자가 자산 검색해서 추가하면 ohlcv 가 비어 있어 백테스트 불가). **수정**: ① `_LoggingEnqueuer` 를 실제 백필 실행 enqueuer 로 교체 — `app/scheduler/backfill_queue.py` 의 `BackfillQueue` 인스턴스를 `app.state.backfill_queue` 로 lifespan 에서 주입 + `enqueue(asset_id)` 호출 시 worker 가 `backfill_asset(session, source, asset)` 호출. ② source 라우팅: market=KR → pykrx, market in (US,CRYPTO) → yfinance (TASK-022 매핑 재사용). ③ POST /api/assets 응답의 `note` 에 "백필 진행 중 (예상 N분)" 표시 + frontend 가 backfill_enqueued=true 일 때 polling. ④ 단위 테스트: 자산 추가 → enqueue 호출 → worker 가 backfill_asset 실행 → ohlcv rows > 0 (mock source 로). **DoD**: 신규 자산 등록 후 5분 이내 ohlcv 가 채워짐 + 백테스트에서 사용 가능. | coder | agent | TODO | HIGH | TASK-022, TASK-031, TASK-213 | 2026-04-30T10:30 | 2026-04-30T10:30 |

## 사용자 의도 정합성 (2026-04-30 두 번째 사용 후 발견)

발견: (1) 백테스트 실행 시 화면 전환되어 폼 입력값 휘발 → 매번 재입력 (UX 결함). (2) MA 필터가 `rebalance_schedule` 일자에만 평가 → 사용자 의도 ("이평선 깨면 즉시 청산, 회복하면 즉시 매수") 와 어긋남. → MA 를 filter 대신 신규 allocator 로 분리 (모델 명확화 + 기존 9 골든 영향 0). signal_filters 전면 재설계는 별도 후일 (사용자 보류 결정).

| ID | Title | Assignee | Execution | Status | Priority | Depends On | Created | Updated |
|----|-------|----------|-----------|--------|----------|------------|---------|---------|
| TASK-218 | DONE — **UX 인플레이스 결과 표시** — 백테스트 실행 시 화면 전환 없음, 폼 값 보존, 결과 자동 표시. **현재 상태 실측**: `frontend/app/backtests/new/page.tsx` = **539 lines** (1071 은 commit 36d4684 의 +/-stat 합산). 결과 컴포넌트 (`frontend/components/backtest/`): EquityChart, DrawdownChart, MetricsTable, MonthlyHeatmap, ProgressPanel, TradesTable, StrategyParamsForm, AssetWeightMap, FilterConfigBuilder, UniverseSelector. polling 인프라 이미 완비 — `frontend/lib/api/client.ts` 의 `getBacktest`/`getBacktestResult`/`cancelBacktest` (L188/194/201) + `useBacktestPolling` (1초 폴링 + terminal 자동 정지) hook. localStorage 사용 0 hit (키 충돌 X). **기존 `/backtests/[run_id]/page.tsx` (290 lines) 는 유지** — 이력에서 결과 다시 보기 진입 경로. **수정**: ① page.tsx 를 좌(폼, sticky lg breakpoint 이상) + 우(결과 패널) 2-column 으로. lg 미만은 stacked. ② 실행 버튼 → POST /api/backtests → run_id 받아 같은 페이지 폴링. running 동안 ProgressPanel (router.push 제거된 버전), done 시 EquityChart + DrawdownChart + MetricsTable + MonthlyHeatmap + TradesTable 자동 렌더 ("결과 보기" 버튼 제거). ③ **ProgressPanel.tsx:89, L139 `router.push('/backtests/{run_id}')` 두 곳 제거** — 자동 navigate 차단. 이력 진입 경로는 별도 (앱 메인 이력 카드 → /backtests/[run_id]). ④ 폼 값 localStorage 보존 — `useFormPersistence` hook 분리 (`frontend/hooks/useFormPersistence.ts`). 키: `backtest:last_form_state:v1`. 저장 필드 9개: { universe_asset_ids: number[] (asset_id 배열만, Asset entity 객체는 제외 — 카탈로그 변동 가능), allocator_name: string, allocator_params: object, filter_configs: object[], rebalance_schedule: string, period_start: string, period_end: string, base_currency: string, initial_cash: object }. 복원 시점: 폼 컴포넌트 마운트 직후 useEffect. **저장 시점**: onChange debounce 500ms. ⑤ 같은 폼에서 파라미터 일부만 바꿔 즉시 재실행 가능 (run_id 만 갱신, 폼 state 보존). ⑥ persona harness 테스트 갱신 (`tests/e2e/test_persona_first_use.py` 가 router.push 동작 가정 시 → polling 동작으로 변경). useFormPersistence hook 단위 테스트 (`frontend/hooks/__tests__/useFormPersistence.test.ts`) — save/restore/version migration. **DoD**: (a) 시나리오 실행 → URL 변경 없음 + 결과 자동 표시, (b) 새로고침 후 폼 값 복원, (c) 파라미터 1개만 바꿔 재실행 → 폼 유지, (d) 이력에서 /backtests/[run_id] 진입 시 기존 결과 화면 정상 동작 (회귀 0). **commit msg 가이드**: "TASK-218: 백테스트 인플레이스 결과 — ProgressPanel router.push 제거 + useFormPersistence 추가 + 좌+우 레이아웃". | coder | agent | TODO | HIGH | - | 2026-04-30T11:00 | 2026-04-30T11:30 |
| TASK-219 | DONE — **MA 시그널 allocator 신규 (`ma_signal`)** — signal_filters 미사용 별도 allocator. **현재 구조 실측**: `backend/app/domain/allocators/` (base.py, fixed_weight.py, all_weather.py, equal_weight.py). FixedWeight 패턴: `fixed_weight.py:23-86` (`FixedWeightParams(BaseModel)` + `class FixedWeight(AllocatorBase[FixedWeightParams])` + `name: ClassVar[str] = "fixed_weight"`). `AllocatorBase[P]` 정의: `allocators/base.py:33-81`. `normalize_weights(...)`: `allocators/base.py` (allow_cash_slot=True 옵션 보유). RebalanceSchedule Literal: `strategy.py:34-41` (이 태스크 schedule 변경 X). **수정**: ① `backend/app/domain/allocators/ma_signal.py` 신규 — `MaSignalParams(BaseModel)` { window: int (default 120, ge=2, le=500), assets: dict[int, float] (asset_id → 비중, FixedWeightParams 동일 검증: non-empty + non-negative + total ≈ 1.0 ± 0.05) } + `class MaSignal(AllocatorBase[MaSignalParams])` { name="ma_signal", params_schema=MaSignalParams }. ② `generate_weights(universe_asset_ids, prices_until_d, signal_date)`: **자산 단위 fallback** (`filters/moving_average.py:52-54` 패턴) — universe ∩ params.assets 각 자산에 대해 (i) prices_until_d[aid].dropna() 후 len ≥ window 인지 확인, (ii) 부족 자산은 skip (cash 처리), (iii) 충분 자산은 close[-1] > SMA(window) 면 params.assets[aid], 아니면 0. 마지막에 `normalize_weights(filtered, allow_cash_slot=True)`. **전체 빈 dict 는 모든 자산이 부족/MA 아래일 때만**. ③ **allocator 등록 매핑 2 곳 모두 갱신**: (a) `backend/app/services/backtest_runner.py:53-57` `_ALLOCATORS` dict 에 `"ma_signal": MaSignal` 추가. (b) `backend/app/api/strategies.py:14-21` import 추가 + `L45-66 list_strategies` 응답 list 에 ma_signal 메타 (이름/한글 설명/params_schema JSON) 추가. (c) `backend/app/domain/allocators/__init__.py` 에 re-export 추가 (있으면). **한 곳 누락 = frontend 드롭다운에서 안 보임 (사일런트 회귀)**. ④ 단위 테스트 `tests/domain/test_ma_signal_allocator.py`: (a) 모든 자산 MA 위 → 입력 비중 그대로 normalize, (b) 모든 자산 MA 아래 → 빈 dict (cash-only), (c) 일부 위/아래 → 위인 것만 normalize, (d) **자산 단위 fallback** (BTC 는 window 충분, ETH 는 부족) → BTC 만 평가됨. ⑤ **frontend 변경 사실상 0** — `lib/api/schemas.ts:182` allocator_name 이 `z.string()` free string 이고 `StrategyParamsForm` 이 JSON Schema 자동 폼 + `complexFieldRenderer` 패턴 (`page.tsx:91-102` `findObjectParamKeys`) 라서 ma_signal `assets: dict[int,float]` 는 FixedWeight `weights` 와 동일 메커니즘으로 AssetWeightMap 자동 주입됨. **단 수동 검증**: api/strategies.py 의 list_strategies 응답에 ma_signal 등장 확인 + 브라우저에서 드롭다운에서 선택 가능 확인. ⑥ architecture.md L670 (V3 백엔드 모듈 분할 표 allocators 행) 에 ma_signal 추가 명시. **L499 MVP 프리셋 카운트 갱신 안 함** (Quant Lab CLAUDE.md L26 = 사용자 명시 추가 결정 영역). **DoD**: (a) 단위 테스트 4 케이스 통과, (b) e2e BTC 100% + ma_signal w=120 + quarterly + 2017-2026 → 분기마다 BUY/SELL, (c) 기존 9 골든 영향 0 (회귀 통과), (d) GET /api/strategies 응답에 ma_signal 등장. **commit msg 가이드**: "TASK-219: ma_signal allocator 신규 — _ALLOCATORS dict + list_strategies 양쪽 등록 + JSON Schema 자동 폼". | coder | agent | TODO | HIGH | - | 2026-04-30T11:00 | 2026-04-30T11:30 |
| TASK-220 | DONE — **RebalanceSchedule 에 `semi_annual` (반기) 추가** — 사용자 요청 (반기 옵션). **사용자 결정 (2026-04-30)**: semi_annual = **1월·7월 첫 거래일** trigger (quarterly 와 의미론 일관). **현재 실측**: `strategy.py:34-41` Literal 6 값, `schemas/backtest.py:29-36` 동기 6 값, `engine.py:133-136` quarterly 분기 = `(d.month-1)//3` 변경 검사 → **분기 시작 첫 거래일** (1/1, 4/1, 7/1, 10/1) trigger (3/31·6/30 분기말 아님). 한글 라벨 위치: `app/backtests/new/page.tsx:74-80` `REBALANCE_OPTIONS` inline 상수 + `L507-521` 드롭다운. `lib/i18n/ko.ts` 에 `rebalance.*` namespace **0 hit** (전수 grep) — 다른 옵션도 inline 한글, 일관성 위해 inline 한 줄 추가. **수정**: ① `strategy.py:34-41` Literal 에 `"semi_annual"` 추가 (7 값으로). ② `schemas/backtest.py:29-36` Literal 동기. ③ `engine.py:133-136` quarterly 분기와 같은 패턴으로 semi_annual 분기 추가 — `if schedule == "semi_annual": return prev_d is None or (d.month - 1) // 6 != (prev_d.month - 1) // 6 or d.year != prev_d.year` (1월·7월 첫 거래일 trigger). 휴일 보정 로직 추가하지 않음 — `_is_rebalance_day` 는 이미 거래일만 받음 (`trading_days_in_period` 결과). ④ frontend `lib/api/schemas.ts:149-156` Zod enum 에 `"semi_annual"` 추가 한 줄. ⑤ `app/backtests/new/page.tsx:74-80` `REBALANCE_OPTIONS` 에 `{ value: "semi_annual", label: "반기" }` 한 줄 추가 (i18n/ko.ts 갱신 X). ⑥ 단위 테스트 `tests/domain/test_engine.py` (현재 `_is_rebalance_day` 테스트 부재 확인됨) 신규: (a) 1월 첫 거래일 trigger, (b) 7월 첫 거래일 trigger, (c) 다른 월 false, (d) 동일 분기 두 번째 호출 false. **휴일 보정 케이스 제거** (calendar 가 처리). **DoD**: (a) 단위 테스트 4 케이스 통과, (b) UI "반기" 선택 가능, (c) 10년 백테스트 시 trades 가 1월·7월 부근만 → 약 20회 안팎. **commit msg 가이드**: "TASK-220: RebalanceSchedule semi_annual 추가 — 1월·7월 첫 거래일 trigger (quarterly 와 일관)". | coder | agent | TODO | MEDIUM | TASK-218 | 2026-04-30T11:00 | 2026-04-30T11:30 |

### 후속 (Coder/Tester 발견)

| ID | Title | Assignee | Execution | Status | Priority | Depends On | Created | Updated |
|----|-------|----------|-----------|--------|----------|------------|---------|---------|
| TASK-221 | DONE — **test_api_contract.py allocator 카운트 갱신** (TASK-219 후속). `tests/api/test_api_contract.py:78-94` 의 `assert len(allocators) == 3` → `== 4` + `'ma_signal' in {a['name'] for a in allocators}` 추가. Tester 가 직접 처리 (3 passed). | tester | agent | DONE | MEDIUM | TASK-219 | 2026-04-30T11:30 | 2026-04-30T11:50 |
| TASK-222 | DONE — **persona harness allocator 하드코딩 갱신** (TASK-219 후속, Tester 발견). `tests/e2e/test_persona_first_use.py:92-110` 에 동일 3종 하드코딩 → 4종 + 'ma_signal' 포함 단언으로 갱신. 단순 1~2줄 변경. | tester | agent | TODO | MEDIUM | TASK-219 | 2026-04-30T11:50 | 2026-04-30T11:50 |

### 실행 순서 권고 (TASK-218/219/220, Reviewer 1차 NEEDS_REVISION 반영)

- **TASK-218 (page.tsx 대규모 재구성)** 과 **TASK-220 (page.tsx REBALANCE_OPTIONS 한 줄 추가)** 가 같은 파일 수정 → **순차 진행**: 218 DONE 후 220 시작 (TASK-220 Depends On = TASK-218).
- **TASK-219 (allocators/ma_signal.py 신규 + backend services/api)** 는 page.tsx 안 만짐 (frontend 변경 0) → 218 과 **병렬 가능**.
- 진행 순서: (218 ∥ 219) → 220.
- Reviewer 3 병렬 (사전 검증) 가능.

### 실행 순서 권고 (Reviewer 지적 5 반영)

1. **순차 1**: TASK-212 (TradeFill 모델 변경) → TASK-211 (engine 청산 — 새 fills 가 settlement_date 채워야 함)
2. **순차 2**: TASK-213 (yfinance/pykrx 에 earliest_available 메서드 추가) → TASK-214 (yfinance auto_adjust=True + pykrx 코멘트 갱신 — 같은 파일 충돌 회피)
3. **병렬**: 순차 1과 순차 2 는 서로 독립 (engine/trade vs data/sources) → 병렬 실행 가능
4. **마지막**: TASK-215 (골든 baseline 재생성) — TASK-211/212/214 모두 DONE 후 단일 책임자
5. **사용자 액션**: TASK-216 (BTC 재백필) — TASK-213 DONE 후 Manager 가 사용자에게 통지

## UI/UX 정공법 (사용자 첫 사용 시 발견 — JSON 직접 입력 사고 방지)

| ID | Title | Assignee | Execution | Status | Priority | Depends On | Created | Updated |
|----|-------|----------|-----------|--------|----------|------------|---------|---------|
| TASK-200 | **AssetWeightMap 위젯 신규** (`frontend/components/backtest/AssetWeightMap.tsx`). props: `universe: Asset[]`, `value: dict<asset_id, number>`, `onChange`. 동작: universe 의 각 자산을 표시 (symbol + 한글명 + market 배지) + 각 자산 옆 슬라이더(0~100%) + 숫자 입력 + 합계 표시 + "균등 분배" 버튼 + "현금 잔여 100%-합계 자동" 옵션. 합 ≠ 100% 면 amber 경고. **JSON-string 입력 영구 금지**. StrategyParamsForm 의 dict 분기를 이 위젯으로 교체. **Acceptance test**: 빌드 산출물에 `weights JSON.parse` 같은 안티패턴 grep 0 + 사용자가 universe 자산만으로 비중 입력 가능 (FixedWeight) 동작 확인. | coder | agent | DONE | HIGH | - | 2026-04-29T14:00 | 2026-04-29T14:30 |
| TASK-201 | **FilterConfigBuilder 위젯 신규** (`frontend/components/backtest/FilterConfigBuilder.tsx`). props: `availableFilters: StrategyDescriptor[]`, `value: FilterConfig[]`, `onChange`. 동작: "+ 필터 추가" 버튼 → 필터 종류 드롭다운 → 선택된 필터의 params_schema 기반 폼 (StrategyParamsForm 재사용) → 필터 row 별 삭제. 다중 필터 = AND 결합 안내 표시. NewBacktestPage 에 통합 (현재 `filter_configs:[]` 고정 → 이 위젯 사용). | coder | agent | DONE | HIGH | TASK-200 | 2026-04-29T14:00 | 2026-04-29T14:30 |
| TASK-202 | **e2e 페르소나 harness** — Manager 가 통합 후 비개발자 사용자 첫 사용 시나리오를 직접 click-through 검증. 시나리오: (1) /assets 접근 → KODEX 200 검색 → 발견 (2) /backtests/new → FixedWeight 선택 → universe 에 SPY+TLT 추가 → AssetWeightMap 으로 60/40 → 기간 2022-01-01~ → KRW → 실행 (3) ProgressPanel 진행률 → done (4) /backtests/[run_id] 결과 시각화 확인. headless playwright 또는 단순 HTTP+HTML grep 양쪽 OK. backend/tests/e2e/test_persona_first_use.py. **Failure replay**: 이번에 발생한 weights symbol 입력 깨짐 + JSON-string fallback 케이스를 회귀 테스트로 영구 박제. | tester | agent | DONE | HIGH | TASK-201 | 2026-04-29T14:00 | 2026-04-29T15:00 |
| TASK-203 | **framework Harness Engineering 가이드 추가** (main 브랜치). agents/coder.md 에 "UI/UX 원칙 위반은 severity=bug 로 분류 (observation 금지)" + "dict/array 파라미터 입력은 전용 위젯 신규 별도 태스크로 분리 필수". CLAUDE.md Step 4 에 "observation → bug 자동 승격 카테고리" 명시 (UI/UX 원칙·실거래 70% 원칙·보안 위반). CLAUDE.md Step 6 에 "통합 후 e2e 페르소나 시나리오 직접 click-through 검증" 의무화. → main d691a86 push 완료. | manager | user | DONE | MEDIUM | TASK-202 | 2026-04-29T14:00 | 2026-04-29T15:30 |
| TASK-204 | **AssetWeightMap 사전 경고 — 매수 불가능 자산 amber 알림**. NewBacktestPage 또는 AssetWeightMap 이 universe 자산의 최저 단위 가격 (정수 자산은 직전 가격, 코인은 무관) × 비중 > 초기 자본 일 때 amber 배너로 사전 안내. 예: "BTC 현재가 ≈ $50,000 — 초기 자본 $10,000 + 비중 100% 로는 매수 불가. 자본을 $50,000 이상으로 늘리거나 비중을 줄이세요". /api/assets/{id}/ohlcv 마지막 close 1건 lookup 으로 가격 조회. | coder | agent | DONE | HIGH | - | 2026-04-29T16:00 | 2026-04-29T16:30 |
| TASK-205 | **코인 한정 fractional 허용** (Q8 재결정, architecture.md V3 § "거래 정책" L611-615 갱신 완료). backend `app/domain/portfolio.py` Portfolio.buy/sell 가 currency=='CRYPTO 자산' 인지 분기 (또는 market 'CRYPTO'). Position.qty 와 BacktestTrade.qty 컬럼을 Numeric(20,8) 로 통일 (alembic 0004 마이그레이션). 정수 자산은 여전히 정수 강제 (Decimal 이지만 int 부분만), 코인은 8자리 소수점. 골든 스냅샷 9 케이스 갱신 (CRYPTO 시나리오 새 결과). e2e Failure replay 추가 (BTC $10k 초기 자본 → 0.2 코인 매수 가능 검증). | coder | agent | DONE | HIGH | - | 2026-04-29T16:00 | 2026-04-29T16:30 |

## 리팩토링 / 클린코드 정리 (2026-04-30 코드 감사)

V3 Phase 1 MVP 완료 후 backend/frontend 전수 감사 결과 식별된 리팩토링 후보. 사용자 요청 "소스 살펴보면서 리팩토링하거나 클린코드로 수정할게 있는지 확인해봐" → 일괄 등록 + 병렬 진행 결정.

### 백엔드

| ID | Title | Assignee | Execution | Status | Priority | Depends On | Created | Updated |
|----|-------|----------|-----------|--------|----------|------------|---------|---------|
| TASK-230 | **source 어댑터 헬퍼 추출** — `backend/app/data/sources/yfinance_source.py:32-68` 와 `pykrx_source.py:31-74` 가 `_RATE_LIMIT_SLEEP_SEC / _rate_lock / _last_call_monotonic / _rate_limit / _is_nan / _safe_float / _is_invalid_close` 동일 헬퍼 복붙. **수정**: `backend/app/data/sources/_helpers.py` 신규 → `class RateLimiter(min_interval_sec)` + `safe_float(v) -> float | None` + `is_invalid_close(v) -> bool` 추출. 어댑터별 sleep 차이는 RateLimiter 인스턴스 인자로 표현. **DoD**: 두 어댑터의 헬퍼 코드 약 80~100줄 감소 + 기존 어댑터 단위 테스트 (`tests/data/test_yfinance_source.py`, `test_pykrx_source.py`) 0 회귀. **commit msg**: "TASK-230: source 어댑터 공통 헬퍼 추출 — RateLimiter / safe_float / is_invalid_close". | coder | agent | DONE | HIGH | - | 2026-04-30T13:00 | 2026-04-30T15:00 |
| TASK-231 | **allocator weight dict 검증 통합** — 실측 차이: `backend/app/domain/allocators/fixed_weight.py:38-52` 와 `ma_signal.py:53-67` 은 **4단계** (non-empty → non-negative → total>0 → abs(total-1.0)≤0.05). `all_weather.py:60-72` `_validate_weights` 는 **3단계** (non-negative → total>0 → abs(total-1.0)≤0.05) — empty 검사는 `Field(default=...)` 가 제공하므로 생략, 별도 `_validate_categories` (L74-79) 가 `asset_categories` empty 처리. **수정**: `backend/app/domain/allocators/_validation.py` 신규 → `validate_weight_dict(v: dict, *, name: str, total_tolerance: float = 0.05, allow_empty: bool = False) -> dict` 1 함수. fixed_weight / ma_signal 호출 시 `allow_empty=False`, all_weather 호출 시 `allow_empty=True`. **DoD**: 단위 테스트 (`tests/domain/test_allocator_validation.py`) — 5 분기 (빈 dict 거부 / allow_empty=True 통과 / 음수 / 합계 0 / 합계 0.9 0.95 1.0 1.05 1.1) + 3 allocator 회귀 통과. **commit msg**: "TASK-231: allocator 가중치 검증 _validation.py 로 통합 — allow_empty 옵션". | coder | agent | DONE | HIGH | - | 2026-04-30T13:00 | 2026-04-30T15:00 |
| TASK-232 | **`_persist_results` 분해** — `backend/app/services/backtest_runner.py:202-286` (실측 85줄) 가 단일 함수에 3 책임 (equity 변환+drawdown / trades 변환 / metrics 계산+flatten+status). 단위 테스트 부재 (`tests/services/test_backtest_runner.py` 0). **수정**: ① `_build_equity_rows(equity_curve) -> list[tuple[...]]` (peak 추적 + drawdown 즉석 계산 포함). ② `_build_trade_dicts(fills) -> list[dict]`. ③ `_compute_and_flatten_metrics(equity_curve) -> dict[str,float]` (annual/monthly flatten 포함). `_persist_results` 는 세 함수 호출 + repo.insert + status 업데이트만. **DoD**: 세 함수 단위 테스트 (`tests/services/test_backtest_runner.py` 신규) + 골든 9 케이스 0 회귀. **commit msg**: "TASK-232: _persist_results 3 함수로 분해 — equity_rows / trade_dicts / flatten_metrics". | coder | agent | DONE | HIGH | - | 2026-04-30T13:00 | 2026-04-30T15:00 |
| TASK-233 | **calendar_guard 사적 심볼 import 정리** — `backend/app/data/pipeline.py:29` `from app.domain.asset.calendar_guard import _MARKET_CALENDARS` 가 밑줄 prefix 사적 dict 직접 import (호출처 L67 `_MARKET_CALENDARS.get(market)`). **수정**: ① `backend/app/domain/asset/calendar_guard.py` 에 `get_calendar_name(market: str) -> str | None` public 함수 신규 (**unknown market 일 때 `None` 반환 — raise 금지**, pipeline.py L67 의 `.get()` graceful fallback 의도 보존. 기존 `_resolve_calendar_name` (L31-34) 은 ValueError raise — 동작이 다르므로 그것의 단순 public 승격 금지. 새 함수는 별도 정의). ② `pipeline.py:29` import 변경 + `pipeline.py:67` `cal_name = get_calendar_name(market)` 로 호출 변경. ③ `_MARKET_CALENDARS` 자체는 모듈 내부 상수로 유지. **DoD**: 기존 테스트 0 회귀 + grep "_MARKET_CALENDARS" 외부 호출처 0 + unknown market 입력 시 None 반환 단위 테스트. **commit msg**: "TASK-233: calendar_guard.get_calendar_name 공개 (None graceful) — 사적 import 제거". | coder | agent | DONE | HIGH | - | 2026-04-30T13:00 | 2026-04-30T15:00 |
| TASK-234 | **페이지네이션 total 정확화** — `backend/app/api/assets.py:144` `total=len(items)` + `backend/app/api/backtests.py:287` `total=len(runs)` 가 limit 분만 카운트해 클라이언트 페이지 수 산정 불가 (assets.py 본문 주석에서도 자인). **수정**: ① **`backend/app/data/asset_repository.py`** (실측 위치 — `repositories/` 하위 아님, `app/data/` 직속) 에 `count(...)` 메서드 추가 (현 search 와 동일 필터 적용). ② `backend/app/data/repositories/backtest_repository.py` (이쪽은 `repositories/` 하위 맞음) 에 `count_runs(...)` 추가. ③ assets.py / backtests.py 라우터가 list + count 양쪽 호출. **DoD**: 기존 API 계약 테스트 통과 + 신규 단위 테스트 (`tests/api/test_pagination.py`) — items=3 + total > 3 케이스. **commit msg**: "TASK-234: 페이지네이션 total 별도 count 쿼리 — len(items) 부정확 수정". | coder | agent | DONE | HIGH | - | 2026-04-30T13:00 | 2026-04-30T15:00 |
| TASK-235 | **source 라우팅 단일화** — `backend/app/api/assets.py:85-86` (KR→Pykrx, else→Yfinance) 와 `backend/app/scheduler/cron_jobs.py:48-52` (`sources_by_market` dict, KR/US/CRYPTO 명시) 가 동일 라우팅 두 곳에 중복. **수정**: `backend/app/data/sources/__init__.py` (또는 `factory.py`) 에 `get_source_for_market(market: str, *, yfinance: YfinanceSource, pykrx: PykrxSource) -> DataSource` 추가. assets.py / cron_jobs.py 양쪽이 호출. **DoD**: 신규 시장 추가 시 한 군데만 수정. 기존 테스트 0 회귀. **commit msg**: "TASK-235: source 라우팅 factory 함수로 통합". | coder | agent | DONE | MEDIUM | TASK-230, TASK-234 | 2026-04-30T13:00 | 2026-04-30T15:50 |
| TASK-236 | **registration enqueue 실패 silent swallow 제거** — `backend/app/domain/asset/registration.py:165` `except Exception: enqueued = False` 가 로그 없음 (V3 silent fallback 원칙 위반). **수정**: ① `import logging` + module logger. ② `except Exception as exc: logger.warning("backfill enqueue failed asset_id=%d symbol=%s: %s", saved.asset_id, saved.symbol, exc); enqueued = False`. ③ 가능하면 BaseException 분리해서 KeyboardInterrupt/SystemExit 는 re-raise. **DoD**: 단위 테스트 (`tests/domain/test_registration.py`) — enqueue mock 이 raise 할 때 logger.warning 호출 검증 + enqueued=False 반환. **commit msg**: "TASK-236: registration enqueue 실패 logger.warning — silent swallow 제거". | coder | agent | DONE | MEDIUM | - | 2026-04-30T13:00 | 2026-04-30T15:00 |

### 프런트엔드

| ID | Title | Assignee | Execution | Status | Priority | Depends On | Created | Updated |
|----|-------|----------|-----------|--------|----------|------------|---------|---------|
| TASK-237 | **BacktestResultView 컴포넌트 추출** — Phase 1 (TASK-238) 후 라인 시프트 정정: `frontend/app/backtests/new/page.tsx:717-786` (`BacktestResultPanel` 함수, 786줄 파일) 와 `frontend/app/backtests/[run_id]/page.tsx:215-283` (인라인 JSX, 284줄 파일) 가 동일 시각화 블록 (EquityChart/DrawdownChart/MetricsTable/MonthlyHeatmap/TradesTable) 두 번. **수정**: `frontend/components/backtest/BacktestResultView.tsx` 신규 — props: `result: BacktestResult` (`@/lib/api/types` 에서 import — Phase 1 결과), `logScale: boolean`, `onLogScaleChange: (v: boolean) => void`, `layout?: "compact" | "full"` (**`compact`** = `new/page.tsx` 의 세로 stack 레이아웃, **`full`** = `[run_id]/page.tsx` 의 `md:grid-cols-2` 병치 레이아웃). `new/page.tsx` 의 `BacktestResultPanel` 함수 삭제 + `<BacktestResultView ... layout="compact" />` 호출. `[run_id]/page.tsx` 의 인라인 JSX → `<BacktestResultView ... layout="full" />` 호출. logScale state 는 호출 페이지 보유. **DoD**: (a) 두 페이지 동일 결과 화면 회귀 0 (수동 시각 비교 또는 build PASS), (b) `frontend/components/backtest/__tests__/BacktestResultView.test.tsx` snapshot 1건 — 디렉토리 부재 확인됨, 신규 생성 OK. vitest+testing-library 설치돼 있음 (hooks/__tests__/ 동일 패턴). (c) `npx tsc --noEmit` 0 + `npm run build` PASS. **commit msg**: "TASK-237: BacktestResultView 추출 — new/[run_id] 결과 패널 통합". | coder | agent | DONE | HIGH | TASK-238 | 2026-04-30T13:00 | 2026-04-30T15:50 |
| TASK-238 | **BacktestResult / Asset 타입 통합 + Zod schema input/output 정리** — 실측 차이: `Awaited<ReturnType<typeof api.getBacktestResult>>` 트릭은 `app/backtests/new/page.tsx:36` 와 `app/backtests/[run_id]/page.tsx:45` 2곳. **`UniverseSelector.tsx:27-29` 의 트릭은 `Awaited<ReturnType<typeof api.listAssets>>["items"][number]` — `listAssets` 트릭 (다른 API)**. 근본 원인 공통 = `lib/api/schemas.ts` 의 `z.record(z.any()).default({})` (8회 = 7 코드 + 1 주석) 가 input/output variance. 또한 **`as` 캐스트 위치 정정**: `StrategyParamsForm.tsx` 에 `as` 캐스트 0건 (`grep " as "` 0 hit), 실제 캐스트는 `frontend/app/backtests/new/page.tsx:96` (`(descriptor.params_schema?.properties ?? {}) as Record<string, { type?: string }>`). `StrategyParamsForm.tsx:32-44` 자체 `JsonSchemaProperty / JsonSchemaObject` 인터페이스 손코딩은 별개 정리 대상. **수정**: ① `frontend/lib/api/types.ts` 신규 → `BacktestResult`, `Asset` (또는 `UniverseAsset`), `StrategyDescriptor`, `JsonSchemaProperty`, `JsonSchemaObject` 등 export. ② `schemas.ts` 의 `z.record(z.any()).default({})` → `z.record(z.unknown()).optional()` 로 통일. ③ `StrategyDescriptorSchema.params_schema` 를 명시적 `JsonSchemaObjectSchema` 로 + `StrategyParamsForm.tsx` 가 그 타입 import. ④ `new/page.tsx:36`, `[run_id]/page.tsx:45` 가 `BacktestResult` import + `UniverseSelector.tsx:27-29` 가 `Asset` import (트릭 주석 제거). ⑤ `new/page.tsx:96` 의 `as` 캐스트 제거 (params_schema.properties 가 이미 `JsonSchemaProperty` 타입이면 캐스트 불필요). **DoD**: `tsc --noEmit` 0 에러 + as 캐스트 1건 제거 + 3 호출처의 `Awaited<...>` 트릭 주석 0. **commit msg**: "TASK-238: BacktestResult/Asset 타입 lib/api/types.ts 로 통합 + Zod variance 정리". | coder | agent | DONE | HIGH | - | 2026-04-30T13:00 | 2026-04-30T15:00 |
| TASK-239 | **`/new/page.tsx` 794줄 분해** — 실측: useState 9 (`L161,204,208,209,213,215,216,217,259`) + useEffect 4 (`L162,220,260,291,333` 중 4 effect — universe hydration / result fetch / latestPrices prefetch / weights sync) + `eslint-disable react-hooks/exhaustive-deps` 4회 (`L192,252,288,364`). **수정**: ① 4 hook 추출 — `frontend/hooks/useUniverseHydration.ts`, `useLatestPriceCache.ts`, `useWeightsUniverseSync.ts`, `useBacktestResultFetch.ts`. ② 좌측 폼 5 카드 분리 — `frontend/components/backtest/forms/StrategyCard.tsx`, `UniverseCard.tsx`, `WeightsCard.tsx`, `FiltersCard.tsx`, `PeriodCapitalCard.tsx`. ③ page.tsx 는 hook 호출 + 카드 조립 + submit 핸들러만 (목표 250줄 이하). ④ `weightsTotal` 합산 중복 제거 (`page.tsx:367-375` ↔ `AssetWeightMap.tsx:167-171`) — child 가 onChange 시 callback 으로 total 노출하거나 한 곳만 계산. **DoD**: page.tsx ≤ 280줄 + 회귀 0 (e2e `tests/e2e/test_persona_first_use.py` + `tests/e2e/test_failure_replay.py`) + eslint-disable exhaustive-deps 0. **commit msg**: "TASK-239: /new/page.tsx 794줄 분해 — 4 hook + 5 카드 sub-component". | coder | agent | TODO | HIGH | TASK-237 | 2026-04-30T13:00 | 2026-04-30T13:00 |
| TASK-240 | **inline 한국어 i18n 통합** — `lib/i18n/ko.ts` 에 namespace 있으나 다수 컴포넌트가 hardcode. 우선순위 영역: `app/backtests/new/page.tsx` 19곳 (L69, 396, 464, 477, 498, 527, 543, 572, 589, 644, 678, 688, 739, 750, 764, 770, 775, 781, 787) + `app/backtests/[run_id]/page.tsx:153,164,175,197,208-211` + `UniverseSelector.tsx:38,87,90,120,127-129,166,171` + `FilterConfigBuilder.tsx:83,147,164,172` + `AddAssetDialog.tsx:110,136,142-147,150,159,175,181` + `app/page.tsx:42,50,54` + `AssetWeightMap.tsx:221-222,237,322,345,351,357,365`. **수정**: `ko.ts` 에 `common.{select,searching,loading,cancel,submit}`, `universe.*`, `assetType.*`, `rebalance.*`, `result.*` namespace 추가 (REBALANCE_OPTIONS 6개 라벨 포함). 각 컴포넌트가 `t()` 호출. **DoD**: grep '"[^"]*[가-힣]' (한글 문자열 리터럴) 컴포넌트 평균 hits 50% 이상 감소 + 사용자 화면 동일 (회귀 0). **commit msg**: "TASK-240: inline 한국어 ko.ts namespace 통합". | coder | agent | TODO | MEDIUM | TASK-239 | 2026-04-30T13:00 | 2026-04-30T13:00 |
| TASK-241 | **useApiErrorToast hook 추출** — `toast(ApiError)` 보일러플레이트 8회 (`new/page.tsx:241,306,438`, `[run_id]/page.tsx:68`, `UniverseSelector.tsx:66`, `AddAssetDialog.tsx:86`, `ProgressPanel.tsx:91,149`). `traceId.slice(0,8)` 와 `slice(0,16)` 길이 불일치 (ProgressPanel 만 16자). **수정**: ① `frontend/hooks/useApiErrorToast.ts` 신규 → `(err: unknown, fallbackTitle: string) => void` 또는 `lib/api/client.ts` 에 `formatApiError(err)` helper. ② `TRACE_ID_PREFIX_LEN = 8` 상수 (`lib/api/client.ts`). ③ 8 호출처가 hook/helper 호출. **DoD**: trace 길이 불일치 0 + 호출처 코드 50% 감소. **commit msg**: "TASK-241: useApiErrorToast hook — toast(ApiError) 보일러플레이트 통합". | coder | agent | TODO | MEDIUM | TASK-239 | 2026-04-30T13:00 | 2026-04-30T13:00 |
| TASK-242 | **죽은 코드 정리** — ① `frontend/app/page.tsx:53` `<Link href="/backtests">` → `app/backtests/` 아래 라우트 부재 (404). 제거 또는 placeholder 라우트 추가. ② `frontend/lib/i18n/ko.ts:78,82` `progress.doneRedirecting`, `progress.seeResult` (TASK-218 이후 인플레이스 전환으로 사용처 0) + L31 `asset.backfilling` (사용처 0) 제거. ③ `frontend/components/backtest/StrategyParamsForm.tsx:54-63` `complexFieldRenderer` prop — 호출 부모 0 (실측 grep 확인) → prop 정의 + L176 호출부 제거. ④ `frontend/lib/api/client.ts:146-150` `getAssetOhlcv` — `getAssetLatestPrice` 가 같은 endpoint inline 호출, 외부 미사용 → `getAssetLatestPrice` 가 `getAssetOhlcv` 재사용하도록 refactor. **DoD**: 죽은 link/key/prop 0 + tsc 통과 + 기존 테스트 0 회귀. **commit msg**: "TASK-242: 죽은 코드 정리 — /backtests link / ko.ts 미사용 키 / complexFieldRenderer prop / getAssetOhlcv 재사용". | coder | agent | TODO | MEDIUM | TASK-239 | 2026-04-30T13:00 | 2026-04-30T13:00 |
| TASK-243 | **markets 상수 통합** — KR/US/CRYPTO 옵션이 5 컴포넌트에 손코딩 (`new/page.tsx:68-72` BASE_CURRENCY_OPTIONS, `UniverseSelector.tsx:36-41` MARKET_OPTIONS, `AddAssetDialog.tsx:130-132`, `AssetTable.tsx:63`, `AssetWeightMap.tsx:74-78`). **수정**: `frontend/lib/constants/markets.ts` 신규 → `MARKET_VALUES: readonly Market[]` + `marketLabel(m): string` + `BASE_CURRENCY_OPTIONS` 등. 5 컴포넌트가 import. **DoD**: 시장 enum 손코딩 1군데로 + 회귀 0. **commit msg**: "TASK-243: markets 상수 lib/constants/markets.ts 로 통합". | coder | agent | TODO | MEDIUM | TASK-239 | 2026-04-30T13:00 | 2026-04-30T13:00 |

## 백테스트 엔진 회계 결함 (2026-05-06 검증 harness 발견 — BUG)

발견 경로: `backend/scripts/validation/run_all.py` (Plan C 4-Layer 검증 harness, 11/11 PASS) + L4 Opus 4.7 sanity 가 짚은 의심점 → 사용자 검토 후 BUG 등급 확정 (architecture.md "모델 A: D 종가 시그널 → D+1 시가 체결" 의도와 어긋남, 스노우볼 효과 우려).

핵심 결함 (engine.py L211-313 흐름 실측):
- D iteration 안에서 ① 시그널 판정 (D 종가까지) → ② settlement_d=D+1 → ③ `execute_rebalance` *즉시* 실행 (portfolio.cash/positions 변경) → ④ 같은 D iteration 안에서 EOD equity 기록 (D 가격으로 *post-rebalance* portfolio 평가).
- 결과: D 일 EOD 평가에 "D+1 가격으로 산 새 포지션 + D+1 settlement 직후 cash" 가 D 가격 기준으로 평가됨 — 실거래 회계 (D EOD 시점에는 *아직 거래 전* 이어야 정합) 와 어긋남.
- 영향:
  - **매도 시그널의 D EOD**: 사용자 기대 = 아직 주식 보유한 평가 / 현 엔진 = 이미 cash + 거래비용 차감.
  - **매수 시그널의 D EOD**: 사용자 기대 = 아직 cash / 현 엔진 = 이미 새 포지션 (D+1 가격으로 산 것을 D 가격으로 평가).
  - 종가↔시가 갭이 큰 자산 (BTC 등) + 장기 백테스트 (수년) 에서 일별 회계 편향이 누적 (스노우볼) → MDD/Sharpe 측정값에 의미 있는 편향 가능.
- 검증 산출물: `signal/stock-backtest/validation-report.md` (S1 Opus suspicions 항목 참조).

| ID | Title | Assignee | Execution | Status | Priority | Depends On | Created | Updated |
|----|-------|----------|-----------|--------|----------|------------|---------|---------|
| TASK-244 | **엔진 EOD equity 기록 시점 결함 수정 — 큐잉 패턴으로 D+1 settlement 분리** — `backend/app/domain/engine.py:204-329` `run_backtest` 의 메인 루프 재구성. **현재 흐름**: `for d in timeline: if rebalance_day(d): target_weights → settlement_d=D+1 → execute_rebalance NOW (portfolio mutate) → record equity at d (post-mutation + d prices)`. **수정 흐름**: `pending_rebalance: dict | None = None; for d in timeline: ① if pending_rebalance is not None: execute_rebalance(portfolio, pending_rebalance, prices=오늘 가격(d), settlement_d=d) → 어제 시그널을 오늘 settlement; pending_rebalance = None. ② record equity at d (post-체결 + d 가격) — Day 0 은 pure cash, Day 1 부터 post-trade. ③ if rebalance_day(d): pending_rebalance = apply_filters_and_allocator(strategy, ..., d) — 다음 iteration 에서 체결.` 이렇게 하면 D EOD 평가가 (a) 어제 시그널이 오늘 settlement 된 직후 상태 + (b) 오늘 가격 → 사용자 멘탈 모델 ("매도 시그널 D EOD = 아직 보유, 매수 시그널 D EOD = 아직 cash") 정합. **세부 수정**:  ① `engine.py` 메인 루프 위와 같이 재구성. `prev_d` 갱신 위치 유지. `cancel_check` / `progress_callback` hook 위치 보존. ② `next_trading_day` 호출 제거 (큐잉 패턴은 D+1 자체가 다음 iteration 의 d). ③ 마지막 iteration 에서 큐잉된 pending_rebalance 가 있으면 settlement 누락 — 정책 결정 필요: (a) timeline 마지막 D 의 시그널은 체결 안 됨 (실거래에서도 마지막 영업일 시그널은 유효 X), (b) 또는 별도 처리. **(a) 권장**. ④ 기존 단위 테스트 `tests/domain/test_engine.py` 의 progress / cancel / timeline 빈 케이스는 영향 없음. ⑤ `tests/regression/test_lookahead.py` 의 D+1 노출 invariant 는 그대로 통과 (큐잉 패턴이 더 엄격하게 차단). ⑥ **골든 스냅샷 9 케이스 영향**: `final_equity` / `mdd` / `cagr` / `sharpe` 모두 미세 변동 가능 — `tests/golden/snapshots/*.json` 9 파일 baseline 재생성 필수. git diff 로 변경 양상 검증 (변경 부호·크기 합리성). ⑦ **validation harness 9 케이스 (L1+L2+L3) 영향**: `backend/scripts/validation/case_l1.py` 의 `closed_form_initial_buy` 매핑 갱신 — 현재는 "Day 0 EOD 가 buy 직후 + D 가격" 가정인데 수정 후 "Day 0 EOD 가 pure cash, Day 1 EOD 가 buy 직후 + Day 1 가격" 으로 변경. case_c1~c5 모두 expected 값 재계산 필요. case_l2 의 hand-traced docstring 도 갱신 (Jan 29 → Feb 2 trace). case_l3 의 invariants 그대로. **DoD**: (a) `engine.py` 메인 루프 재구성 + 단위 테스트 신규 (Day 0 EOD = pure cash 검증, Day 1 EOD = post-init-trade 검증, 매도 시그널 D EOD = 아직 보유 검증, 매수 시그널 D EOD = 아직 cash 검증), (b) 기존 회귀 테스트 (lookahead, calendar_defense, cash_by_ccy) 0 회귀, (c) 골든 9 케이스 baseline 재생성 + 변경 부호 검증 (수치는 변할 것이나 부호는 동일 자릿수 수준), (d) validation harness L1~L3 갱신 + 11/11 재달성, (e) `architecture.md` § "시그널 / 체결 시점 — 모델 A" 에 "EOD equity 기록 시점 = pre-rebalance / post-체결 흐름" 명시 (TASK-244 와 동시 commit). **commit msg 가이드**: "TASK-244: engine EOD 회계 fix — pending_rebalance 큐잉 패턴 (D 시그널 → D+1 settlement) + 골든 baseline 9 재생성". | coder | agent | TODO | HIGH | - | 2026-05-06T05:00 | 2026-05-06T05:00 |

### 의존성 그래프 / 병렬 실행 계획 (Reviewer PASS 후 갱신)

Reviewer 사전 검증에서 **TASK-237 ↔ TASK-238 의 page.tsx import 블록 hunk 인접 충돌** 위험 지적 → 두 태스크 순차 (TASK-238 → TASK-237) 로 변경.

**Phase 1 (동시 7개 — 서로 다른 파일)**:
- TASK-230 (sources/_helpers.py 신규 + yfinance/pykrx 변경)
- TASK-231 (allocators/_validation.py 신규 + 3 allocator)
- TASK-232 (services/backtest_runner.py)
- TASK-233 (data/pipeline.py + asset/calendar_guard.py)
- TASK-234 (api/assets.py + api/backtests.py + 2 repository)
- TASK-236 (domain/asset/registration.py)
- TASK-238 (frontend lib/api/schemas.ts + types.ts 신규 + StrategyParamsForm.tsx + 3 호출처 import 갱신)

**Phase 2 (Phase 1 완료 후, 동시 2개)**:
- TASK-235 (api/assets.py — 234 후) + TASK-237 (BacktestResultView 추출 — TASK-238 후) — backend/frontend 영역 분리 → 병렬 가능

**Phase 3 (TASK-237 완료 후, 단독)**:
- TASK-239 (`/new/page.tsx` 794줄 분해)

**Phase 4 (TASK-239 완료 후, 순차 권장)**:
- TASK-240 / 241 / 242 / 243 모두 page.tsx + 다수 컴포넌트 만짐 → 같은 파일 충돌 다수.
- 240 → 242 → 241 → 243 순 (파일 변동량 큰 순)
